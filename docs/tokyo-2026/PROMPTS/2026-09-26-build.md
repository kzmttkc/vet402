# 2026-09-26: handing over two sends, and how the story is told

The decisions I made on 2026-09-26. The live sends I confirmed with `y` on this day are listed with
their blocks in [`../human-log.md`](../human-log.md), not here. The policy for this folder is in
[`README.md`](./README.md). Times are JST.

### 1. Hand the AI the `y` for the re-signing run before the deadline (06:57)

The attestations to be placed that morning would be older than the agent's 24-hour window by the time
judging starts, so a second run of the purchases and the attestations was planned for 2026-09-27 07:45.
It needed my `y`. My answer:

> これはあなたができるのでは？

("Isn't this one you can do?") The AI runs it without my `y`, only on the JST date 2026-09-27; every other
check before a send stays in place.

- Landed in: `535bafd4` (`examples/tokyo-2026-demo/src/lib/env.ts`)

### 2. The video and the submission text are a presentation, not a list of facts (07:44)

Summary of my instruction: tell it in four parts (setup, answer, turn, close) so the viewer is drawn in.
Say briefly why ENS and Intercepta, how they work here, and how vet402 keeps using them. Inside that,
put the facts that actually ran, short and exact.

- Landed in: the video and the submission text (outside git)

### 3. My voice, English subtitles, a heading card (09:40 to 10:23)

I recorded the narration myself, in four recordings (09:40 to 09:49). At 09:48 I asked for English
subtitles. At 10:23 I asked for a heading card at the start of scene S06, the scene that explains why
ENSv2.

- Landed in: the video (outside git)

### 4. Take in every finding of the adversarial check, and a safety run of the re-signing (10:38)

I told the AI to take in every finding of an independent adversarial check of the submission. At the
same time I handed it the `y` for one more re-signing run, on 2026-09-26 at 19:00, ahead of the one at
2026-09-27 07:45.

- Landed in: commits after `e572257c` (`git log e572257c..tokyo-2026-submission`)
