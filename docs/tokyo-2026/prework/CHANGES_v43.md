# PLAN_v4.2 → PLAN_v4.3 の差分（畳み込みの記録）

- 作成: 2026-09-19 JST。**読み取りと文書編集だけ。実装 0・commit 0・送信 0**
- v4.2 は「末尾に追記節は無い」と書いて出したが、**同じ日のうちに追記が5本また積み上がった**。追記は本文の数字・手順・関門を上書きする内容なので、このままだと当日に本文を上から読む実装者が古い指示で動く。
- v4.3 は**追記5本を全部本文へ畳み込み、追記節を消した**版。節の構成（1〜10）は v4.2 のまま。
- 畳み込んだ追記（**5本**）:
  1. `## 09-19 15:1x 3回目の敵対監査（審査員・規約）の反映 —— 受賞を落とす級6件`（J1〜J6・P1〜P9・F-06〜F-08・L3）
  2. `## 09-19 15:2x 3回目の敵対監査（技術）の反映 —— 止まる級2件`（A1・A2・B1〜B9・C1）
  3. `## 09-19 15:3x 審査員ボタンの関門を「書ける形」に確定`（控え `guards/GUARDS.md`・597行）
  4. `### 09-19 15:4x ガスの確定と、残る危険7件`（控え `guards/GAS.md`・332行・`GAS.json`・`SELLER_D.json`）
  5. `### 09-19 15:4x 予行演習の一式を「どこでも走る形」に作り直した`（控え `rehearsal/`）
- 畳み込みの規則: **後の決定が勝つ**。古い言い方・古い数字・存在しない関数名は本文に1つも残さない。ただし **§10.6「使ってはいけない値・語」の表は禁止語そのものを載せる場所**なので、そこには残す（機械検査の対象がその表である）。
- 本文の分量は 1,554 行 → **1,877 行**。増えた主因は §3.7.1（関門6つの実装可能な形）・§4（seller-d 一式と鍵ごとのガス）・§10.6（禁止の表が 56 行 → 約 100 行）。

---

## 1. 節ごとの差分

| 節 | 何を変えたか | なぜ（出どころ） |
|---|---|---|
| 冒頭 | 版を v4.3 に。参照コミットを **`d350540` → `e8ff1eb`**（`pay-or-refuse.ts` は無変更なので grep アンカーは有効）。控え（`guards/`・`rehearsal/`）を明記 | 追記3 §9.5・追記4 の危険4 |
| **§1.4** | **本命を $4,000（Continuity 専用）に決めた**（2枠出しの根拠は「禁じる文が見つからない」だけ）。**ENS の欄は2本書く**。U7 を打つ枠を **K1-post へ移した**。U10 のボタンが壊すのは **`seller-d.eth`**。U11 に「**live demo のリンクは最小提出でも外さない**」 | 追記1 J5・P4・P9・P7・J3 |
| **§1.6** | やらないことに2つ追加: **審査員ボタンに `seller-a.eth` を壊させること**／**予約ラベル（`atst`・`obs`）に `setResolver` / `unregister` を打つこと** | 追記1 P7・追記2 B2 |
| **§1.7** | 「他の参加者の名前で1回買う」の**手順を書いた**（相手に頼む署名は登録・`x402-offer`・証明の3つだけ。**委任は頼まない**。証明は相手が置く。gas は【未確認】） | 追記1 P6 |
| **§2.4** | **`eth_simulateV1` を出すのは sentio だけ**の表を追加（ethpandaops は `-32601`・25 秒かけて落ちる／**予備は3本目 `0xrpc.io/sep`**）。**A3 の 20/20 の組は読み取りの組であって simulate の組ではない**・**ガスは1系統の値**と明記 | 追記5 の誤り2 |
| **§2.5** | 表に **`seller-d.eth`（未登録・`isAvailable = true`）**の行。家賃は **MockUSDC 8.000021 / 1年**・**ETH 建ての `getRegisterPrice` は revert**・`MIN_COMMITMENT_AGE` 60 秒 | 追記3 §9.4・追記4 §3 |
| **§2.7** | SHA を **`e8ff1eb`** に。`input.payee` を「4 か所」→ **「7 行当たり、置き換えるのは 4 か所」**。**npm の事実**（`@vet402/sdk` の latest は 0.6.0・`@vet402/tokyo-demo` は 404）。**App 側のテストは `npx tsx --test`**（174 本が `@/` を使う）。**`~/vouch/node_modules` が空**・**手元の node は 26.3.0 で本番と CI は 24.x** | 追記1 P5・追記2 B5・追記3 §1/§9・追記4 の危険2/3 |
| **§2.8** | **版管理の規律**（大きな単一コミットは既定で失格扱い）。**Continuity の下位トラック**（`Extend Open Source` / `Ship a Feature`）を提出時に確認。**提出後〜審査中は請求パスに触らない**。**Finalist は狙わない** | 追記1 J4・F-06/07/08 |
| **§3.3.1** | **`output.required` のキー名を決めた**（`["result","observed_at"]`）。逐語の 266 バイトの約束を本文に置き、K1-04/07 のガスの基準を固定。売り手 route（B4a）はこの2キーを返す義務を負う | 追記4 の危険1 |
| **§3.3.2** | 判定コマンドを**行番号の除外つき**に（`grep -c 'input\.payee'` が `0` にはならない。読み取り4か所だけを見る2行に書き換え） | 追記2 B5 |
| **§3.3.3** | **G5b に4枝目**を追加: `recommendation` が `allow`/`warn`/`block` のどれでもない 200 も拒否（`{}`・`null`・`{"ok":true}` は JSON として読めるので既存3枝では捕まらない）。言い方の表に **`vet402 API: unreachable (asked, no answer)`** の行 | 追記2 B4・追記1 P2 |
| **§3.4** | `admin.ts` の下位コマンドを **8 → 9**（**`register-d`** を新設。60 秒の待ちもコマンドの中で待つ）。`deploy-resolvers` が **`P_d`** も作る。`k1b` に **BS-03** | 追記3 §9.4・追記4 §6 |
| **§3.5** | 手当てを **3 → 4**。**予約ラベルは `register` を塞ぐだけで、`setResolver` 1本（48,235 gas）で同じ乗っ取りが起きる**。禁止を `register`・`setResolver`・`unregister` に広げ、**T7 を live で打つ**（唯一の構造的な閉じ方） | 追記2 B2・B7 |
| **§3.7** | 面を4 route に（`verify` / `mutate` / **`reset`** / **`state`**）＋ `_lib/`。**DDL 2表**（`tokyo_mutations`・`tokyo_mutation_log`）。`force-dynamic` の義務。ボタンが壊すのは **`seller-d.eth`** | 追記3 §7/§8/§10 |
| **§3.7.1** | **関門6つを「書ける形」に全面差し替え**。W01＝**許可リスト4つと完全一致**（「1つだけ」は達成不能）＋import グラフ／W02 そのまま／W03＝**Postgres の1文 upsert ＋ `runtime_flags.tokyo_button_halt`・上限 60 回**／W04＝**固定値の比較ではなく式**／W05＝**「次に来た人が来たときに戻す」**（`after()` の `setTimeout` は本番で発火しない実測）／W06＝**書き込み先の4点がソース定数**（審査ウィンドウは撤回）。判定は **`npx tsx --test tests/tokyo-mutate.test.ts`**、置き場は **`$WT/tests/`**。`ensureReverted` の5段・止め方の SQL 1文・押した後の画面・会期中の作業順9段を追加 | 追記2 A1・追記3 全体 |
| **§3.8** | 検証コマンドを **`npx github:kzmttkc/vet402#tokyo-2026-submission verify seller-a.eth`** に（`@vet402/tokyo-demo` は npm に無い）。clone からの1行も併記 | 追記1 J1 |
| **§3.9** | 売り手 route が **`result` と `observed_at` を返す義務**。attester の手順(4)も同じキーを引く | §3.3.1 と同期 |
| **§4** | **全面改訂**。①**合計 gas を1つの数字で正典にしない**と宣言し、**鍵ごとの実測合計と関門**（W_vet 2,998,514/4,200,000・W_ens 3,546,604/4,800,000・W_op 157,012/3,300,000）を正典に ②**seller-d 一式 5 行（K1-D1〜K1-D5・1,178,390）を表の行として追加** ③K1-04 **575,239**・K1-07 **861,493**・K1-12 **462,532**・K1-post **168,049/168,037**・D-1 **78,506**・D-6a **72,604**・D-6b **132,797** に差し替え ④**K1-15c/15d を K1-post へ移動** ⑤**BS-03 を 0.05 ETH** に ⑥本数を **Sepolia 27 tx ＋ Base Sepolia 2** に ⑦余裕の根拠（最大の1本を打ち直せる額）と安全係数 3 の置き方 ⑧census の期待に `P_d.roles(...)` と `4 offers` | 追記3 §9.4・追記4 §0〜§6 |
| **§5** | mutate テストの行を **`$WT/tests/tokyo-mutate.test.ts`＋`.pg.test.ts`・`npx tsx --test`** に。T41b を**4枝**に。**数え方を「一意の ID」に**（行数では 77 を返す）。**B1 も B6 も期待は 56**（v4.2 の「B6 は 52 以上」を廃止）。走らせ方に App 側の tsx の2行と `npm ci` の注意 | 追記2 B6・追記3 §9 |
| **§5.5** | 宿題を **H1〜H14** に（**H14＝node 24 と `node_modules`** を新設）。**完全に閉じたのは H4・H6・H8・H9・H12 の5件**（H6 を足した）。**09-24 の判定を「ファイルが在る」→「`node rehearsal/run.mjs --all` が exit 0」**に差し替え、終了コードの意味・RPC の差し替え・`--record` の禁止・合否に使わないもの（gas とブロック番号）を追加。**予行演習の作り直しで見つかった検証の誤り6件**を表に | 追記2 A2・追記5 全体 |
| **§6.1** | K1 を **Sepolia 27 tx** に。B0 に「**npm ci と node 24 から**」。**K1 ブロックの中の順**（approve と commit を最初に打ち、60 秒の待ちを K1-01〜03 で消化。K1-post は B5b の後ろへ回せる）を新設 | 追記3 §10・追記4 §3 |
| **§6.2** | ①18:30 の枠に **`npm ci`** と `node -v` ②B0 の最初の3行 ③B1 の判定を **9語**と**一意の ID 56** ④K1 の行に **seller-d の4行を飛ばさない**・`register-d` の待ち ⑤B5a を `seller-a,b,c,d`・B5b を **4本** ⑥B7 の最後に **T7 を live で打つ**（B7 の期待文字列に `register(agent-1)` と `emancipate(T7)` を追加）⑦13:45 の手順を §1.7 の5段へ ⑧判定点③を **`npx tsx --test`** ⑨**B0 の gas の関門を `gas-budget.mjs` の3鍵に**（数字を本文から消し、定数ファイルを1か所の正典に）⑩**監視の直しを3 → 4つ**（`/tokyo` の反転を **2026-09-26T06:30:00Z** に・**自前 `node_modules`**）⑪規律に**「大きな単一コミットを作らない」** ⑫バッファに ⑦（K1-post を後ろへ回せる）| 追記1 J4・追記2 A2/B3/B7・追記3・追記4 §7 |
| **§7** | 落とさないものに **`/tokyo` の URL そのもの**と **seller-d 一式**を追加。E3 から seller-d を除外（審査員ボタンの前提） | 追記1 J3・追記3 §9.4 |
| **§8.1** | **冒頭3行を差し替え**（1行目と2行目に **ENSv2**・2行目に **Enhanced Access Control**・**em ダッシュを削除**）。2 に **npm の latest は 0.6.0** の書き方。5b の `output.required` を確定値に、検証コマンドを **github: 形式**に。**7 を ENS の欄2本**（$6,000 用と $4,000 用の逐語）。**10 を新設**（開示は送った・返信は無い） | 追記1 P1・P3・P5・J1・P4/J5・規約の穴 |
| **§8.2** | 0:00–0:30 の画面と台詞を **`vet402 API: unreachable (asked, no answer)`** に（**`vet402 API calls: 0` と "it never asked me" を削除**）。0:00 の台詞に **ENSv2**。末尾に「**ボタンを撮るのは B9b・壊すのは `seller-d.eth`**」 | 追記1 P1・P2・P7 |
| **§8.3** | K1 の tx 一覧を **Sepolia 27 本＋Base Sepolia 2 本**に。ボタンの押し方に **`seller-d.eth`** と「`seller-a.eth` は無傷」。CLI を **github: 形式**に。`FEEDBACK-ENS.md` は「提出後に公開」と書いたリンクだけ | 追記1 J1・P7・P8 |
| **§8.4** | **時期の表を「書く＝凍結前（B10）／出す＝提出後」に1つ決めた**（3つの節で食い違っていた）。F6 に「**予約だけでは塞がらない・`setResolver` 1本で乗っ取れる**」を追記 | 追記1 P8・追記2 B2 |
| **§8.5** | **`PLAN_v4.3.md` 全文を prework に入れない**と決め、**公開版 `prework/PLAN_PUBLIC.md`（§2〜§5・§10 の抜粋）**を新設。公開しない側に **`guards/`** を追加。`TOKYO_PATHS` に **`tests/tokyo-mutate*.ts`・`tests/helpers/env-graph.ts`・`scripts/sql`** を追加。**`git mv` と修正を分ける** | 追記1 J6・追記2 B8・J4 |
| **§8.6** | 開示メールと計画の**食い違い3件**を表にし、(a) を閉じた（改名は完了済み）。口頭確認を **3点 → 4点**（下位トラックを追加）。**返信 0 通のまま提出する**と明記 | 追記1 の規約の穴 |
| **§9 R4** | 本命を $4,000 に置いたことが Q1 の答え次第でひっくり返る、を1行 | 追記1 J5 |
| **§9 R5** | 表題に「提出後に `/tokyo` の経路が動く」を足し、**L3 の申し送り**（`/api/tokyo/seller` と `/tokyo` に提出後〜結果発表まで触らない・**申し送りは 09-24 までに vet402.com の製品セッションへ**）を追加 | 追記1 L3 |
| **§9 R6** | 兆候を「52 本に届かない」→「**一意の ID が 56 に届かない**」 | §5 と同期 |
| **§9 起きたら止める** | **「審査員ボタンを止めたくなった」**の行（`/api/admin/spending-halt` を流用しない） | 追記3 §4.2 |
| **§9 最小提出** | 手順1に「**1つの大きなコミットにしない**」。手順2を **「live demo のリンクは絶対に外さない。同じ URL に静的な最小ページを出す」**に差し替え。落としてよい順の先頭を「審査員ボタン」に、落としてはいけないものに **`/tokyo` の URL が 200** を追加 | 追記1 J3・J4 |
| **§10.1** | **`P_d`**（`0x9CF7990dAB364d1738ABB09532b953Ec48269831`）・**`seller-d.eth`**・**W_op の役割と【未確認】**を追加 | 追記3 §9.4・追記4 |
| **§10.2** | 本番 env を**4つの表**に（`DATABASE_URL`・`TOKYO_OPERATOR_PRIVATE_KEY`・`TOKYO_SEPOLIA_RPC_URL`・`TOKYO_JUDGE_BUTTON_DISABLED`）。**使ってよい既存 lib は3本だけ**。`.env.tokyo.local` に **`TOKYO_W_OP_ADDRESS`**。`TOKYO_PATHS` を §8.5 と同じ1文字列に更新 | 追記3 §2.3 |
| **§10.3** | `x402-offer` と証明キーの置き場に **`seller-d.eth`**、書ける鍵に **`P_d` の上の W_op** | 追記3 §9.4 |
| **§10.5** | **`output.required`**・**審査員ボタンの定数一式**（chainId・4点・床・`PRESS_GAS`・`GAS_SAFETY`・**1日 60 回**・`REVERT_AFTER_MS` の意味）・**鍵ごとのガスの関門**・**BS-03 0.05 ETH**・反転時刻 **2026-09-26T06:30:00Z**・テストの本数 56・参照コミット `e8ff1eb` を追加。**K1 の合計 gas の行を削除** | 追記3・追記4 |
| **§10.6** | 禁止の表を4群（ABI と配備／tx の本数と gas／審査員ボタン／主張・提出物／計器・手順・宿題）に整理し、**約 45 行追加**。主なもの: 合計 gas 4通り・「Sepolia 18 tx」・K1-04/07/12・B5b 418,854・D-1 52,297・`U.unregister` 72,652・戻しの 172,536・888,838/888,874・W_vet 4.0M・W_ens 1.2M/1.6M/2.2M・BS-03 0.02・ボタン 200 回・`seller-a.eth` を壊す・W01「1つだけ」・`ip-rate-limit` の import・KV/Redis/インメモリ・env だけの停止・`/api/admin/spending-halt` の流用・W05「90 秒で自動復帰」・W06「審査時間外は 404」・`node --test` と `$WT/test/`・`@vet402/tokyo-demo`・「npm 公開・0.7.0」・`vet402 API calls: 0`・live demo リンクを外す・`PLAN` 全文を prework に・「20/20 の組で simulate も回る」・反転 13:00Z・09-24 を「ファイルが在る」・`--record` を会期中に・予約ラベルだけで塞ぐ・他リポの `node_modules`・node 26 のまま・`grep -c input.payee → 0`・本数 52・宿題「4件」・`output.required` 未定 | 追記1〜5 全体 |
| **末尾** | 追記5節を削除。「追記は速いので、また積む」と明記し、**残っている【未確認】16 件の一覧**（消し方つき）を置いた | 今回の再発 |

---

## 2. 畳み込みの過程で見つかった**新しい食い違い**（全件・9件）

| # | 何が食い違っていたか | v4.3 でどう決めたか |
|---|---|---|
| **M1** | **審査員ボタンの1日の上限が 60 と 200 で割れている。**`guards/GUARDS.md` は §W03・§4.2 とも **200 回**（`ip_rate_limits` の上限・W03-a の「201 回目は 429」）で書かれているのに、`guards/GAS.md` §6 の資金の実測は **60 回**（0.05 ETH で3日ぶんを安全係数 3 込みで賄える）を推している。**同じ日の2つの控えが違う数を持っている** | **60 を採る**（後の決定・資金の実測が根拠）。§3.7.1 の W03 の本文・テスト（「**61 回目は 429**・`max:60`」）・§10.5 の定数・§10.6 の禁止行を**全部 60 にそろえた**。GUARDS.md の 200 は古い値として扱う |
| **M2** | **`eth_simulateV1` を出す RPC が「sentio だけ」と「sentio と `0xrpc.io/sep` の2本」で割れている。**`guards/GAS.md` §0/§8 は「**受けるのは sentio だけ**・ガスの2系統照合は**できない**」と書くが、`rehearsal/README.md` は同じ日の実測として **`0xrpc.io/sep` も OK**（同じ結果・exit 0・5〜6 秒）と書いている | **両方を成り立たせる形で1つに書いた**: 「**A3 の 20/20 の組（sentio ＋ ethpandaops）のうち simulate を出すのは sentio だけ。予備は3本目 `0xrpc.io/sep`**」。**ガスは1系統の値で、価格と残高だけ2系統で一致**という GAS.md の結論はそのまま残した（§2.4） |
| **M3** | **K1 の合計 gas が、どの版でも一致しない。**v4.2 の 3,597,829 は仮値ベースの 3,388,563 に K1-15b/15c/15d を足した値だが、09-19 の再測で K1-04（+126,723）・K1-07（+322,118）・K1-12（+69,187）が動き、さらに seller-d 一式（1,178,390）が丸ごと抜けていた。**どの1つの数字を採っても間違いになる** | **「合計 gas を1つの数字で正典にしない」と §4 に明記**し、正典を**鍵ごとの実測合計と関門**に移した。**3,388,563 / 3,476,015 / 3,510,377 / 3,597,829 の4つを §10.6 で全部禁止**にした |
| **M4** | **「案A 設置 5 tx の合計は 888,874」という B0 の合格条件（v4.2 §10.6）が、もう成り立たない。**その 5 tx には K1-12 が含まれ、**K1-12 は 393,345 → 462,532 に動いた**ので、888,838 も 888,874 も両方とも古い。v4.2 は「888,838 を使うな・888,874 を使え」という形で**古い値を正解として固定していた** | **両方を禁止にし、B0 の合格条件を `preflight-tokyo.mjs` の `gas_budget.ok` が3鍵とも true に差し替えた**（§10.6・§6.2） |
| **M5** | **`seller-d` の tx 本数が「5本」と「7本」で割れている。**追記3 は「**K1 に5本足す**」と書くが、同じ節の実測表は **7 行**（approve / commit / register / deployProxy / setResolver / grantSetterRoles / B5b-d）である | **5 行の表にまとめた**（K1-D1 が登録の3 tx を1行で持つ。K1-D5 は B5b で打つ）。**tx の本数は「本体 6 tx ＋ B5b で 1 tx」と本文に別記**して、行数と tx 数を混ぜないようにした（§4） |
| **M6** | **W01 の env 許可リストが4つなのに、W03 の停止スイッチの正典が DB の行になっている。**`TOKYO_JUDGE_BUTTON_DISABLED` を許可リストに残す以上、env も読む。**「env は設定であって操作卓ではない」という本家の規律と、env を1つ許可することが噛み合っていない** | **2枚構成として明記**した: **正典は DB の1行**（`runtime_flags.tokyo_button_halt`・SQL 1文で次の署名から効く）、**env は DB ごと落ちたときの最後の手**（再デプロイが要る）。W03-f は `"1"` の1値だけを止めに解釈する（§3.7.1） |
| **M7** | **`git mv` を1コミットにまとめない（J4）と、`git mv` のときに import を同じコミットで書き換える（§5 の規則）が正面から衝突する** | **§5 の規則を残し、§6.2 と §8.5 の規律を「`git mv` と**中身の修正**を分ける」に書き分けた**。import のパスの書き換えは `git mv` に付随する機械的な修正なので同じコミットでよい。**分けるのは「移動」と「機能の修正」**である |
| **M8** | **`/tokyo` の期待の反転時刻が3つある。**v4.1 は 09-25 12:00Z、v4.2 は 09-26 13:00Z、追記2 B3 は **09-26T06:30:00Z**（実際の公開時刻）。v4.2 の 13:00Z は「逆向きに 6.5 時間ずれている」と追記2 自身が書いている | **2026-09-26T06:30:00Z を正典**にし、§6.2 の監視の直し・§10.5 の定数・§10.6 の禁止行・`rehearsal` の `TOKYO_EVENT_START` の4か所をそろえた |
| **M9** | **宿題で「完全に閉じた」件数が 4 件と書かれているが、H6 は同じ表の中で ✅ になっている。**v4.2 §5.5 の本文は「H4・H8・H9・H12 の4件」と書き、表の H6 は「✅ 完全に閉じた」だった | **5 件（H4・H6・H8・H9・H12）に直した**。あわせて **H14（node の版と `node_modules`）を新設**し、09-24 の日割りに閉じ方を書いた（§5.5・§10.6） |

---

## 3. v4.3 に残した【未確認】（**16 件**・本文の末尾に一覧がある）

| # | 場所 | 中身 | いつ消えるか |
|---|---|---|---|
| 1 | §2.1 | `docs.ens.domains` の ENSv2 API 解説ページは JS 描画で機械確認できない | 消さない（チェーンから読む） |
| 2 | §2.3 / §3.1 / §5 U04 | 参照実装 版2 の `p`＝記録キー・`h`＝値の keccak256 という写しの解釈 | 会場の Q2 |
| 3 | §2.6 / §9 R5 | 場面1の5件が当日も ALLOW のままか | 09-26 12:10 UTC |
| 4 | §2.8 | 規約の2つの疑いと、**Continuity の下位トラックがどちらか**（新規） | 09-25 21:40 の口頭確認・プロジェクト作成の画面 |
| 5 | §4 / §10.1 | **W_op のアドレス**（会期中に `keys.ts init` が作る。preflight は `TOKYO_W_OP_ADDRESS` が無ければ skipped）（新規） | BS-03 の直後に preflight をもう1回 |
| 6 | §4 / §10.5 | ガスはその calldata での上限の見積もり。本番の実鍵とは 1 gas 単位で一致しない | 本番で打つまで消えない（消さないのが正しい） |
| 7 | §4 | **Sepolia の baseFee が会期中に跳ねるか**。測ったのは直近 3.4 時間だけで、安全係数 3 は実測の振れから導いた値ではない（新規） | 会期当日の `eth_gasPrice` |
| 8 | §3.7.1 | **審査員が実際に何回押すか**（実績が無い。1日 20〜40 回・3日で最大 120 回と置いた【推定】）（新規） | 審査期間中の `tokyo_mutation_log` |
| 9 | §5.5 H1 | `eth_simulateV1` は sentio（予備 `0xrpc.io/sep`）でしか回らず、回ごとに揺れる | B0 |
| 10 | §5.5 H7 | 受取人スコア 41 / 境界 40 ＝ 余裕1点 | B0 |
| 11 | §5.5 H10 | ENSIP-29 草案 PR #85 は 09-19 時点の値 | 09-24 夜と B0 |
| 12 | §5.5 H11 | app.ens.dev の登録画面の文言 | 09-25 会場 |
| 13 | §5.5 H14 | **手元に node 24 が無い・`~/vouch/node_modules` が空**（新規） | 09-24 昼 |
| 14 | §8.4 | ENS への公開分の件数（4 件か 5 件か） | 送る直前（提出後） |
| 15 | §1.7 | **「他の参加者の名前で1回買う」の gas**（§4 に tx が無い）（新規） | 当日 13:45 |
| 16 | §8.6 | **開示メールの返信**（09-19 から 0 通）（新規） | 来なければ来ないまま提出する |

**消えた【未確認】**: v4.2 の「K1-post 2 本の gas（約 17 万【推定】）」は **168,049 / 168,037 の実測**に置き換わった。「`output.required` のキー名」は **§3.3.1 の決定**で消えた。
