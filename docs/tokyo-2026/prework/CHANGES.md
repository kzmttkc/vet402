# PLAN_v4 → PLAN_v4.1 の変更一覧

- 土台: `.company/departments/hackathon/tokyo-2026/v4/PLAN_v4.md`（branch `kabau-trust-board`・**979 行**）
- 出力: `PLAN_v4.1.md`（**1,389 行**）
- 反映元: 仕組みの理論検証（A1〜A11）と整合性の理論検証（S1〜S7・曖昧 10 件・その他）
- **「行」はすべて `PLAN_v4.1.md` の行番号**（機械で取り直した実測）。古い記述は残していない（置き換えか削除）。

---

## A. 仕組みの検証から（A1〜A11）

### A1 —「vet402 を止めても払える」の言い方を直す

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 1.1 | 18 | オーナー決裁の日本語は変えず、**英訳の規律**を1段落足した（"even while vet402's API is unreachable" であって "without vet402" ではない） | 決裁文は変えられないが、対外の英語が偽になるのを止める必要があった |
| 1.2 | 27 | 軸 (ii) を「**API が返らない**状態（接続不能か 503・`cut-vet402`）」に。「問い合わせに行って返ってこない」であって「問い合わせない」ではないと明記 | SDK 0.7.0 は床を宣言しても `/api/v1/` へ**毎回1回**取りに行く【実測 TM-4】 |
| 1.3 | 35 | Q-B の答えを「API が**返ってこない**状態で払える」に修正 | 同上 |
| 2.7 | 257 | 現物の行を1本追加: 「床を宣言しても毎回取りに行き、返らなければ `evidence_unavailable` で拒否する。G1〜G10 がこの挙動を足す」 | 会期で作るものの範囲が v4 では読み取れなかった |
| 3.3.3 | 444-454 | 前提を書き直し、**「言ってよい／言ってはいけない」の3行表**と **`cut-vet402` の定義（接続不能か 503。404 は当たらない）** を追加 | 404 の HTML では現物も拒否のまま【実測】。撮影と CLI の作り方が決まっていなかった |
| 3.3.3 G5/G6 | 462-463 | G5 を「throw・タイムアウト・5xx の3つだけ」に限定。**G6 の「4xx（404 を除く）」を削除**し「404 を含めて全部免除しない」に | v4 の書き方は「存在しない資源なら払える」抜け道になっていた（**新しく見つけた矛盾 D-1**） |
| 3.8 | 674-675 | `run.ts cut-vet402` の定義（`VET402_API_URL` を接続不能先へ／`--mode 503`。`--mode 404` は作らない）と `--restore` | 撮影・CLI・テストで「止める」の意味を1つにする |
| 8.1 | 1054-1060 | 冒頭3行を書き換え（"The agent still asks vet402 …, but when nothing comes back it pays anyway"）＋「この3行で守る言い方」 | 審査員に「一度も問い合わせない」と読まれると、コードを開いた瞬間に崩れる |
| 8.2 | 1099 | 動画 0:00–0:10 の画と声を「API が接続不能。問い合わせても何も返らない。それでも払う」に | 同上 |
| 10.6 | 1367-1369 | 禁止行に3件追加（「一度も問い合わせない／vet402 抜きで払う」「`cut-vet402` を 404 で作る」「G6 の 404 除外」） | 写し間違いの再発を止める |

### A2 — アンカー表に4行追加（`input.payee` を読む箇所）

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 3.3.2 | 410-440 | **アンカー #17〜#20 を追加**（`:630-638` rail／`:960` `sameAddress`／`:828` subgraph／`:1135` attest）＋直し方（`payeeAddr` に一本化・判定 `grep -c 'input.payee'` → 0）＋ **B3 の作業の先頭**と明記 | `payee` 無しだと `rail="svm"` に落ちて `assertSvmPayer` が throw し、`sameAddress(x, undefined)=false` で必ず `payee_mismatch`。**直さないと場面2・3 が1回も動かない** |
| 2.7 | 259 | 「`input.payee` を読む箇所 = **4 か所**」の行を追加（取り直しコマンドつき） | 差し込みの網羅を数で確かめられるように |
| 6.2 | 947 | B3 受け入れの手順を「**#17〜#20 を最初に読む**」に。判定に `grep -c 'input.payee'` → 0 を追加 | 人の 1 時間をどこから使うかを決める |

### A3 — 命題4（作者が拒まれる）の台本を直す

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 1.5 | 79-87 | 「作者自身が拒まれる」→「**約束を書き換えられる鍵が、証明には手が届かない**」。W_op / W_vet / W_ens の3行表（C-01〜C-08【実測・sim】）を追加 | W_vet が拒まれるのは `P_a` に役割が1つも無いからで、証明キーが特別だからではない（同じ理由で `x402-offer` も書けない） |
| 3.4 | 509-510 | 「確かめ（画面に出す）」を C-01〜C-05 の実測値に。W_vet の件は「台詞に使わない参考」に降格 | 同上 |
| 7 | 1025 | 「落とさないもの」の文言を差し替え | 同上 |
| 8.2 | 1106 | 2:15–2:55 の画と声を **W_op** に（"the key that just changed the price cannot touch the attestation next to it"） | 同上 |

### A4 — 命題5 の罠（親 wildcard が古い方針を蘇らせる）

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 3.2 | 326-327 | `resolveText` / `resolveAddr` を **`{value, resolver}` を返す**形に変更。読み取りを **`UR.resolve` に一本化**し viem の `getEnsText` 等を使わないと明記 | どのリゾルバが答えたかが、罠の判定に要る |
| 3.5.3 | 580-594 | **新設**。罠の実測（scene3 A1：`unregister` 後に `R_vet` 経由で古い方針が返る）と、二重の手当て **(a) 親に書かない (b) リゾルバが `P_AG1` でなければ `agent_policy_missing`** | 取り消しても方針が生き返ると、命題5（U8・取り消しで委任が死ぬ）の主張が崩れる |
| 1.6 | 101-102 | やらないことに「親 `R_vet` に `x402-policy` を書く」「漏れを live の tx で実演する」を追加 | 同上 |
| 4 K1-02 | 705 | 「**`x402-policy` は絶対に書かない**」を注記 | 同上 |
| 3.0 | 294 | 全体図の (0) に「答えたリゾルバが `P_AG1` でなければ `agent_policy_missing`」 | 同上 |
| 10.3 | 1317 | `x402-policy` の行を「`agent-1.vet402.eth` だけ。`vet402.eth` には置かない」に | 同上 |

### A5 — K1-15b を追加

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 4 | 719-720 | **K1-15b を追加**（W_vet → R_vet `grantSetterRoles(x402-policy → K_ag2)`・**87,452 gas**【実測・sim】）＋「漏れの実演は `eth_call` だけ」の注 | K1-15 だけでは `K_ag2` も親のキーで revert（C-18）。**K1-15b が無いと §3.5 の before/after が成立しない** |
| 3.5 | 532 | 見せ場の行を before/after で書き直し、K1-15b が必須と明記 | 同上 |
| 4 判定 | 735 | census の期待の1行に `vet402.eth x402-policy empty` と `R_vet.roles(keccak(x402-policy), K_ag2)=0x10` | (a)(b) を1回で確かめる |
| 6.2 | 948 | K1 を「**18 tx**（Sepolia 16 ＋ Base Sepolia 2）」「**K1-15b を飛ばさない**」に | 同上 |
| 1.4 U9 | 57 | U9 の答えに対照 `agent-2`（K1-15＋K1-15b）を追加 | 賞の加点項目に K1-15b が効く |

### A6 — 場面3の順序と `linkToRecord` の第2引数

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 4 | 743-754 | デモ tx を**順番つきの表 D-1〜D-6** に。**入れ替えると seller-c が seller-b の約束を指したまま残る**（S3alt【実測・sim】）・空の箱にはつなげない（S3trap）・W_op は `linkToRecord` を打てない（trap-4）を明記 | 撮影の最後に「seller-c が他人の約束を名乗る」状態を作らない |
| 1.4 U6 | 54 | `linkToRecord(seller-c, R_b)` → **`linkToRecord(dns("seller-c.eth"), 1)`**（第2引数は recordId） | v4 はリゾルバのアドレスを渡す書き方になっていた |
| 8.2 | 1107 | 2:55–3:25 も recordId `1` に修正し、順序固定を明記 | 同上 |
| 6.2 B7 | 954 | `scene3 ok:` の期待文字列を実際の 13 手順に合わせて書き直し | 判定の文字列が手順と一致していなかった |

### A7 — K1 の gas を実測値に

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 4 | 698-700 | 合計 gas を **3,388,563**（15/15 通し・block 11733994）に。K1-15b 込み **3,476,015**。**ETH の手当ては 4,000,000 gas**（本物の文字列で K1-04 だけ +115,277） | v4 の 3,218,146／3,388,515 は別々の測定の足し算（【推定】）だった。宿題 H2 が閉じた |
| 4 | 715-718 | K1-12 **393,345**／K1-13 **172,536**／K1-14 **87,452**／K1-15 **170,381**／`unregister` **72,652** に差し替え | 通し実測の値 |
| 10.1 | 1285-1286 | **U = `0xB093cEC6D1Cc355c40020f0df5103ecEFfD30dac`／P_AG1 = `0xd3F4818c0bB93e54780525b21380D44D6D06bcd6`** を記入（「戻り値で確定」を置き換え） | 予定アドレスは送り手＋salt で決まる（E4）ので会期前に確定できる |
| 6.2 | 973-986 | **B0 に gas の関門**（`eth_gasPrice × 4,000,000 < W_vet 残高 × 0.5`。W_ens は 1,200,000 の行も） | 足りないまま K1 に入ると途中で止まる |
| 10.5 | 1344-1346 | 定数表に payload 282B・envelope 79B・K1 の合計 gas を追加 | 引き写しの正典 |

### A8 — ENSIP-29 の検証は成立（Format note 3点・`x402-offer` の定義）

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 2.3 | 179-189 | **検証の実測表**（基準 VALID・10 件 INVALID・payload 282B・envelope 79B） | 軸 (i)(ii) が仕組みとして成立していることの一次根拠 |
| 2.3 / 3.1 | 177 / 315 | `atst-me-v2` の **`p`＝記録キー・`h`＝値の keccak256** を明記（**【一次未確認】**・Q2 で確認） | S3 の指摘。v4 はキー順だけ書いて中身を書いていなかった |
| 8.1 | 1067-1070 | Format note に**3点**（`a` は 20 バイトの CBOR bytes／`n` は ENSIP-15 正規化後／EIP-191 の形）を逐語で | 草案が決めていない所をこちらが決めたと名指しする |
| 8.1 | 1073-1088 | **新設 5b**: `x402-offer` の記録キーの定義1枚（草案 42 行「自己検証できる情報に証明を出すな」への答え） | 審査員と ENS の双方に効く論点 |
| 5 U01 / U04 | 790 / 793 | U01 に 282B・79B、U04 に `p`/`h` と digest 差を固定 | 実測値をテストで留める |

### A9 — `x402-policy` のスキーマ確定（H12）

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 3.5.1 | 536-565 | **新設**。7キー確定・知らないキーは拒否・`max` は USDC 最小単位の10進文字列（`/1e6`）・`trust` はローカルとの積集合（アドレスは必ずローカル）・`minL1Deliveries`/`minSubgraphReceipts` は書けない | v4 の `{v,trust,floor,max}` 4キーは案のままだった |
| 3.5.2 | 567-578 | **新設**。`AgentPolicy` → `payOrRefuse` の写し方を1対1の表に | 「方針から通信先を決めさせない」を明文化 |
| 4 K1-12 | 715 | `initialize` の `setText(x402-policy)` の値を §3.5.1 の7キー JSON に | 同上 |
| 5 T49 | 839-840 | T49 を5枝に（(e) リゾルバ違い）＋ (f) として写しの検査を**同じ ID の中**に | 本数 55 を動かさずに写しを固定する |

### A10 — `agent_policy_missing` は `examples/` 側（H9）

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 3.5 | 530 | `examples/` 側と確定し、**理由**（`payOrRefuse` を呼ぶ前に落とすので SDK の決定行に現れない／parity テストが「誰も生まない語」を要求する）を明記 | 宿題 H9 を閉じる |

### A11 — H4 の決定を差し替え（サーバー側の鍵）

| 節 | 行 | 何を | なぜ |
|---|---|---|---|
| 3.7 / 3.7.1 | 624-662 | v4 の「鍵を意図して公開」を**取り消し**、**サーバー側の鍵（案a）**に。関門4つ（専用 env `TOKYO_OPERATOR_PRIVATE_KEY`／`10000⇄10001` の2値／`chainId` と IP 間隔／判定の写し替え）＋画面に W_op と `ResourceArgument`＋**公開案を採らない理由1行**＋開示文 | オーナー承認 09-19 08:5x。公開鍵は同じリゾルバの任意のサブ名に同じキーを書け（C-04）、ガス代を誰でも抜ける |
| 3.7.1 | 651-654 | 新テスト **W01・W02**（`$WT/test/tokyo-mutate.test.ts`）。**T/U の本数には数えない**と明記 | 数の正典（55）を壊さない |
| 10.2 | 1305 | 本番 env に `TOKYO_OPERATOR_PRIVATE_KEY` を1つだけ足すと明記 | 同上 |
| 5.5 H4 | 884 | 宿題表の H4 を新しい決定で ✅ に | `v4/HOMEWORK.md` の古い決定を上書き |

---

## B. 整合性の検証から（S1〜S7）

| # | 節 | 行 | 何を | なぜ |
|---|---|---|---|---|
| S1 | 3.7.1 / 6.2 | 643-645 / 958 | 判定の grep に**パス限定を戻した**（`$WT/src/app/tokyo $WT/src/app/api/tokyo/verify`）。**`mutate` は除外範囲**と明記 | 鍵をサーバー側に置く以上、リポ全体の grep → 0 は必ず落ちる |
| S2 | 3.3.1 | 337-398 | `AttestationStep`・`EnsOfferCheck`・`X402Offer`・`TrustedAttester`・`EnsAttestationPolicy` の**中身を `PLAN_v3.md:270-318` から戻した**（`payeeName`・`network`・`ens` の3入力を含む） | v4 は型名だけで、会期中に設計をやり直すことになっていた |
| S2 | 3.3.2 | 438 | **`payee: string` 必須 union の崩し方を1行で確定**（`Base & ({payee} \| {payeeName; ens; payee?}) & ({account}\|{svm})`） | 既存の呼び手の型と挙動を変えない形を1つに決める |
| S3 | 2.3 / 3.1 | 177 / 315 | `atst-me-v2` の `p`/`h` の意味を戻し、**【一次未確認】**の印を付けた | Q2 で確かめるまで断定しない |
| S4 | 3.3.3 | 469-479 | 新2床と `PayEvidencePolicy.source` の関係を明記（**`source` は変えない・`d350540:1257-1269` の分岐に足さない**）＋ **T45 に枝 (c)** | 「宣言したのに黙って無視される床」を作らない |
| S5 | 3.9 | 683 | `@vet402/sdk` の `exports` に **`"./ens"` サブパス**（束ね口 `ens.ts`）を追加 | 名前を使う呼び手だけが viem を引く形にする |
| S5 | 5 | 822 | **T30b を追加**（`dist/index.js` の静的グラフにも viem が入らない）。**テストは 54 → 55** | 入口が viem を引くと「依存 0 の SDK」の主張が崩れる |
| S6 | 6.2 規律 / 8.5 | 1004 / 1153 | **件名規約 `tokyo: <英語>`** を本文に。判定を `… \| grep … \| wc -l` → `0` に | `grep -c` / `grep -vc` は一致 0 件で exit 1。v4 の3本が `set -e` と CI で落ちる形だった |
| S7 | 5 | 760-778 | **MCP と DEMO のテストの置き場と glob を統一**（red は `test/tokyo/`、本線は `test/tokyo-*.test.mjs`）。ファイル名の表を本線の名前に | v4 は MCP だけ置き場と時期が食い違い、T38 が一度も走らない恐れがあった |
| S7 | 5 / 6.2 | 868-869 / 953 | B6 の判定に **`$MCP`・`$DEMO` の2行**を追加（3パッケージとも `ℹ fail 0`） | 同上 |

---

## C. 曖昧 10 件

| # | 何が曖昧だったか | 節 | 行 | どう確定させたか |
|---|---|---|---|---|
| 1 | `EnsReadClients` の型と偽 client | 3.2 / 5 | 325 / 783 | **`EnsRpc`（`readContract`・`getBlock`・`getBlockNumber`・`getChainId` の4メソッドの構造的な型）**。偽物はこの4つだけを実装しキャスト不要。v4 の「偽物のメソッド名は `findExactOwner` に揃える」は**誤りとして削除** |
| 2 | 読み取りの経路 | 3.2 | 326-327 | **`UR.resolve` に一本化**。viem の `getEnsText`/`getEnsAddress`/`getEnsResolver` は使わない（戻り値の resolver が要る） |
| 3 | `checkEnsOffer` の `profile` の型 | 3.3.1 | 343 | **`ChainProfile`**。使うのは `network` と `asset` の2つだけ。Sepolia の 11155111 は `clients` 側の検査と明記 |
| 4 | `assertEnsAttestationPolicy` の規則 | 3.3.1 | 385-398 | **欄ごとに9行の表**（範囲・既定値・`minValid` の上限・**知らないキーは throw**） |
| 5 | T32 の枝と期待の対応 | 5 | 842-849 | **4枝 (a)〜(d) と期待する理由コードを1対1の表**に（順序も固定） |
| 6 | `AgentPolicy` → `PayPolicy` の写し方 | 3.5.2 | 567-578 | 1対1の表（写さないものも明記） |
| 7 | `initialize` の `calls` の2キー | 4 | 707 / 710 | K1-04 は **`x402-offer` と `agent-endpoint[x402]`**（＋`setAddress`）の3本。**証明キーは入れない**（署名は購入後なので B5b で別に書く） |
| 8 | `trace` の段数と 7 の関係 | 3.3.1 | 383 | **`trace` は常に長さ 7**。止まった後ろは `"skipped"`。「段数」という言い方をしない |
| 9 | `getState` の戻り値の形 | 3.2 | 325 | `{ tokenId: bigint; status: number; latestOwner: 0x…; expiry: bigint }` |
| 10 | prework とタグをどのツリーで打つか | 6.2 | 938-941 / 1002 | **すべて `git -C ~/vouch-tokyo`**。判定も同じツリー。**`~/vouch` のブランチは1回も切り替えない**（打ってよいコマンドを規律に列挙） |

---

## D. その他（切り捨て・パス・時間割・参照）

| 何 | 節 | 行 | 変更 | なぜ |
|---|---|---|---|---|
| `TOKYO_OPT_TESTNET_PAY` | 5 | 823-825 | **T31・T32・T33 に追加** | E2 を発動すると3本が赤のまま残る構造だった |
| §7 と E1・E2 の矛盾 | 7 | 1027-1037 | 軸 (ii) を **ii-a / ii-b / ii-c の3層**に分け、**(ii-a) を落とす切り捨ては存在しない**と明記 | v4 は「軸 (ii) は落とさない」と言いながら E1・E2 でその中身を落としていた（**新しく見つけた矛盾 D-2**） |
| E1・E2 の書き換え | 7 | 1042-1043 | E1＝(ii-c)、E2＝(ii-b) と名指しし、落とし方を env 1つに | 同上 |
| R6 の判定 | 9 | 1243-1244 | 本数 50 → **51**、「軸 (i)(ii)(iii)」→「(i)(iii) と (ii-a)」 | 同上 |
| `TOKYO_PATHS` | 8.5 / 10.2 | 1157 / 1300 | **両方**に `':!docs/tokyo-2026/prework'` を追加し、「この1文字列だけを正典とする」と明記 | 落とすと会期前の設計物が請求範囲に入る |
| `for-reviewers.md` | 8.3 | 1116-1126 | **凍結後に自動再生成**（`gen-for-reviewers.ts --sha $FREEZE`）＋確認コマンド2本 | 凍結前に SHA を書くと審査員が開いた先が別の行になる |
| 撮影とワークショップの重なり | 6.1 / 6.2 | 926 / 955-957 | **B9a を 13:45–15:00** に短縮。**15:20–15:30 に撮り残しの枠**を新設 | v4 は B9a（–15:15）とワークショップ（15:00–15:20）が 15 分重なっていた |
| B4b の前倒し | 6.1 / 6.2 | 920-921 / 945 | **B4b を 06:00–07:00** に（B6 下書きは 06:00 まで） | 03:00〜06:00 に人の判断を置かない規律を保ったまま B3 受け入れ（07:00）に間に合わせる |
| `run.ts verify` の夜間の生存判定 | 6.2 | 946 / 989-993 | B2 直後と B4b 直後に各1回。落ちたら `HANDOFF.md` に書いて次へ渡す | K1 と B5b の判定がこのコマンドに乗っている |
| `ls \| grep -c` → `find \| wc -l` | 6.2 | 953 / 955 / 959 | B6・B9a・B9b の3か所 | `grep -c` は一致 0 件で exit 1 |
| 人の合計時間 | 6.2 | 970-971 | **09-26 13.0h・総計 22.0h**。バッファ ⑤ を追記 | 上の時間の付け替えの反映 |
| 参照コミット | ヘッダ / 2.7 / 3.3.2 | 8 / 252 / 408 | **`5547fe55` → `d350540`**。アンカー 16 本が同じ行にあることを実測で明記 | 提出文と `for-reviewers.md` の行アンカーの土台 |
| 会期前の宿題 | 5.5 | 875-903 | **新設**（§6 の前）。H1〜H13 の担当・期限・「閉じたと言える1行」・状態を `v4/HOMEWORK.md` と同じ形で。**H2・H4・H6・H7・H8・H9・H10・H12 は ✅**。09-20〜09-24 の日別の作業表つき | 会期の 36 時間を実装だけに使うために、卓上で閉じるものを正典の中に置く |
| テストの本数 | 5 / 6.2 | 851-870 / 943 / 952 | **55（B1）／51（B6）** に統一。数え方も `\| wc -l` に | T30b の追加 |
| `admin.ts` の下位コマンド | 3.4 / 6.2 | 501 / 943 | 本文も判定も **8つ／8語**（`agents` を含む） | v4 は本文「7つ（+8つ目）」、判定「7語」で割れていた（**新しく見つけた矛盾 D-3**） |
| 使ってはいけない値・語 | 10.6 | 1365-1379 | **14 行追加**（古い gas・言い方・順番・鍵の公開・`grep -c`・本数・コミット・参照） | 会期中に古い版を写したら止まるようにする |

---

## E. 反映しなかったもの（1件）

| 何 | なぜ |
|---|---|
| 「`x402-policy` の漏れ（K_ag2 が親の方針を書ける）」を **live の tx** で見せること | A5（K1-15b を足す）と A4（親に方針を書かない）を両立させるため、**漏れの実演を `eth_call` / simulate に限定**した。live で書くと §3.5.3 の罠を自分で作ってしまう。賞の見せ場としては simulate の出力で足りる（U7 も同じ扱い）。**指摘そのものは全件反映している**——これは A4 と A5 が衝突する1点の裁定 |

---

## F. この版で新しく見つけた矛盾（v4 の内部・3件）

| # | どこ | 中身 | どう直したか |
|---|---|---|---|
| D-1 | 3.3.3 G5 / G6 | G5 が「届かない＝5xx だけ」、G6 が「4xx（404 を除く）は変えない」と書いており、**404 の扱いが両方から漏れて「存在しない資源なら払える」抜け道**になっていた | G6 を「404 を含めて全部免除しない」に（現物でも 404 は拒否のまま【実測 TM-4】） |
| D-2 | 7 と E1・E2 | 「軸 (ii) は落とさない」と「E1 で届かない経路を落とす／E2 で実支払いを落とす」が正面衝突 | 軸 (ii) を ii-a/ii-b/ii-c に分け、E1＝ii-c・E2＝ii-b と名指し。**ii-a を落とす切り捨ては存在しない** |
| D-3 | 3.4 と 6.2 B1 | `admin.ts` の下位コマンドが本文「7つ（+`agents` で8つ目）」、判定「7語」で割れていた | 両方 **8つ／8語** に統一 |
