# 期待値の差分（旧 → 新）

測り直し: Sepolia `sepolia.rpc.sentio.xyz`・`eth_simulateV1`・2026-09-19 07:3x〜07:4x（head−3）。
署名 0・送信 0。

## 1. ガス（`expected/*.json` には入らない。`--gas` が毎回測る）

| tx | 旧（39B の仮の約束 / 68B の仮の方針） | 新（正典 266B / 178B） | 差 | §4 の表 |
|---|---|---|---|---|
| **K1-04** deployProxy(P_a) | 448,516 | **575,239** | +126,723 | 575,239 一致 |
| **K1-07** deployProxy(P_bc) | 539,375 | **861,493** | +322,118 | 861,493 一致 |
| **K1-12** deployProxy(P_AG1) | 393,345 | **462,532** | +69,187 | 462,532 一致 |
| K1-01〜K1-15 合計 | 3,388,563 | **3,906,591** | +518,028 | — |
| `run.mjs --k1` の totalGas（K1-03b 込み・16 本） | 3,487,475 | **4,005,503** | +518,028 | — |

鍵ごとの実測合計（`--gas` の section 7。§4 の「関門」の列と突き合わせる）:

| 鍵 | 実測合計 | 関門 | 判定 |
|---|---|---|---|
| W_vet | **2,998,514** | 4,200,000 | OK（§4 と一致） |
| W_ens | **3,546,604**（seller-d 一式 1,178,390 ＋ BS-03 21,000 込み） | 4,800,000 | OK（§4 と一致） |
| W_op | **157,012**（押下1回） | 3,300,000 | OK（§4 と一致） |

予定アドレスも §4 の表と一致した【実測】:
`P_a 0xC54403186Db35B9D92cc393Ae665D3960117ac14` / `P_bc 0xd6A089Fc08e5e97697a3293d02F40837CeA1d2bf` /
`P_AG1 0xd3F4818c0bB93e54780525b21380D44D6D06bcd6` / `P_d 0x9CF7990dAB364d1738ABB09532b953Ec48269831`。

## 2. `expected/k1.json`（49 行 → 51 行・変わったのは 5 行、足したのは 2 行）

| 項目 | 旧 | 新 |
|---|---|---|
| `STR x402-offer bytes` | （無し） | **`266`** — 仮値に戻ったら落ちる関門 |
| `STR x402-policy bytes` | （無し） | **`178`** — 同上 |
| `CD-K1-04 calldata keccak` | `0x3242ae58…a2f07be5` | `0x1954be7b…98c56411` |
| `CD-K1-07 calldata keccak` | `0x489abfcd…31b517a5` | `0x394ce215…5e76d982d9` |
| `K1-04` のイベント列 | `[…,TextUpdated,TextUpdated,**TextUpdated**,AddressUpdated,…]` | `[…,TextUpdated,TextUpdated,AddressUpdated,…]`（**calls が4本→3本**・§4「証明キーは入れない」） |
| `C-13 resolve(text agent-1 x402-policy)` | `{"v":1,"trust":[…],"floor":"l2_match","max":"50000"}` | §3.5.1 の7キー（`network`・`floors`・`requireVet402Allow`・`maxAgeSeconds` 入り） |
| `C-19 resolve(text seller-a x402-offer)` | `{"v":1,"amount":"99999"}` | §3.3.1 の 266 バイト（`amount":"10001"`・D-1 と同じ1文字違い） |

`ADDR P_A` / `ADDR P_BC` / `ADDR U` / `ADDR P_AG1` は**変わらない**。
`VerifiableFactory.deployProxy` の番地は (送り手, salt) だけで決まり、`initialize` の中身に依らないため。

`expected/h13.json`（54 行）は `--record` で取り直しても**差分 0 件**。

---

## 3. 2026-09-24 の取り直し（`Label` イベント1本とガス 45,278 が消えた件）

**原因は ENS でも自分たちでもなく、赤の他人だった。**【実測・二分探索15回】

| | |
|---|---|
| 変わった最初のブロック | **11,754,772**（2026-09-22 01:45:12 UTC・JST 10:45）。`agent-2` 側は1本後の 11,754,773 |
| 消えたもの | `U.register` の先頭のログ **`Label(bytes32,string)`**（topic0 `0x4acabfe38b19342d926f219b03cae7c02831d64c4449ccd3d6726b8ef1f99630`・openchain で確認）と **22,639 gas ×2** |
| 何が起きたか | **09-22 の朝に第三者が Sepolia で `agent-1` / `agent-2` のラベルを先に登録した**（tx `0x7c7ca904…`・from `0x4d7257ff…`・こちらとは無関係）。ラベル文字列の共有の箱 `LabelStore 0x375C082021E677a40eA2AE094D050602dba90992` は**そのラベルが初めて使われたときだけ**書き込んでログを出すので、先に埋まったぶん**こちらの初回書き込みが消えた**（cold SSTORE 20,000＋2,100＋ログ代 ≈ 22,639 に一致） |
| コードは変わっていない | 12 アドレスの `eth_getCode` の keccak が**新旧で全部同じ**（`UR_IMPL` も `VerifiableFactory` も `LabelStore` も） |
| 計画への影響 | **無い。**`register` は今も成功し、残る5本のログも同じ。`agent-1` の方針・addr・持ち主の読み取り（C-13・C-14・C-21）も OK。**ガスは安くなった** |
| 逆流 | **しない**（共有の箱は一度埋まったら消えない） |

**ガスの更新**: K1 の合計 **4,005,503 → 3,960,225**（−45,278・−1.1%）。減ったのは **W_vet 側**（K1-13・K1-15 とも `from` は W_vet）なので、W_vet の実測合計は **2,998,514 → 2,953,236**。関門（4,200,000）は変えない。

