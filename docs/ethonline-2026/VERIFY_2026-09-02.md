# 独立検証（2026-09-02 19:0x・ハッカソン戦略セッション）

> 大型アップデートと監査の完了報告を受けて、**報告を鵜呑みにせず自分で測った**記録。
> 作業ツリーは別セッションの未コミット変更があるため触らず、`origin/main` の中身と**本番の実挙動**だけを見た。

## 1. 凍結は保たれている（会期の新規は無傷）

`origin/main` を直接 grep（`packages/` `src/` `examples/`）:

| 会期スコープ | 検出 | 判定 |
|---|---|---|
| `payOrRefuse` | **0ファイル** | ✅ 未実装 |
| `pay_if_trusted` | **0ファイル** | ✅ 未実装 |
| `AgentBook` / `gateway.thegraph.com` | **0ファイル** | ✅ 未実装 |
| `evidence` の `source` | `src/lib/decision/decide.ts` は `{level, url}` / `{level, purchase_id, url}` のみ | ✅ **`source` は会期の新規のまま** |

**紛らわしい一致を2件、実体まで確認した**:
- `agentkit` が20ファイルに出るが、すべて **Coinbase AgentKit**（既存の `examples/agentkit-spend-guard`）。
  World の AgentKit ではない。**提出物では両者を明示的に呼び分ける**——判読を誤らせない。
- `evidence` の一致は配列そのもので、`source` フィールドではない。

リリース後の main は 58 コミット / 138ファイル / +6,615行。**その中に会期対象は1件も無い。**

## 2. デモの経路は生きている

| 相手 | `/decision?role=payer` | 
|---|---|
| kronossignals（払う側） | **ALLOW** `l0_pass, l1_delivered` / L1 3/3 / degraded false |
| agent.api.0x.org（拒む側） | **BLOCK** `l0_unverified, l1_not_attempted` / degraded true |

`rules_version: 2026-09-02.1`。本番 `/health` は ok。**動画の対比はそのまま撮れる。**

## 3. 数字が大きく動いた（撮影当日に必ず再取得）

| | 8/31 | 9/2 |
|---|---|---|
| 登録エンドポイント | 20,488 | **21,445** |
| 機械検証 pass | 1,771 | **3,818** |
| L1 試行 / 成立 | 2,018 / 902 | **2,374 / 980** |
| 直近7日の測定率 | 19.8% | **41%** |

pass が倍増したのは L0 が v2 封筒を読めるようになった是正（6,907件の誤 fail）と cadence 修正の効果。
**これは動画で語れる**——「我々の計器が間違っていた。直したら、通っていた店が倍見つかった」。

## 4. 未解決2件（製品側へ質問・提出前に決着させる）

1. **`publishedFail` = 40 なのに、C1 初回走査の報告は fail 1,014 だった。**
   公開面に出る「fail」の定義（証拠完備の要件か、観測回数の要件か）を確認したい。
   動画で「失敗も同じ重みで公開している」と言うなら、**公開されている失敗の実数**を正しく言う必要がある。
2. **`/decision` の応答時間は東京から warm 0.44–0.49s（cold 0.77s）。**
   §12 の SLO は「p95 < 200ms（キャッシュヒット）」だが、これは**日本からの往復を含む壁時計**であって
   サーバ内時間ではない。**SLO の達成/未達をこの数字で語らない。** サーバ側計測が要る。

## 5. 結論

会期入り（9/4 00:00 UTC）の前提は満たされている。**9/3 のタグを打ってよい。**
