# 2026-09-10（Day 6）— 提出文の主語と humanizer・審査中に `--live` は打たない・実測で確かめてから書く

この日にオーナーが出した判断・指示と、それが入ったコミットの抜粋。会話の全文ではない（方針は [`README.md`](./README.md)）。時刻は JST。

## 判断と反映

### 1. ソロ参加なので、提出文の主語は I（07:03）

> ソロなので主語はIになるべきでは？

- 反映: 提出フォームの文面（git の外）。リポの審査員向け英文は 09-11 に `fa10182` 以降で I にそろえた（[`2026-09-11-day7-health-sawtooth.md`](./2026-09-11-day7-health-sawtooth.md) の 3）

### 2. 提出テキストは必ず humanizer を通す（07:04–07:08）

> 提出テキストにおけるヒューマナイザーの利用は必ずです。

公式文書なのでカジュアルにはしない。ただ、機械的な文の違和感は読み手にすぐ伝わり、それだけで信頼を落とす。

- 反映: 提出フォームの文面（git の外）

### 3. check-in #2 を提出。絵文字の選び方は読み手の解釈で決める（07:26–07:31）

> 絵文字は開発におけるフィーリングを問うものだよ。
> 信号の場合、注意が必要で困難にあたっていると人間は解釈します

- 反映: ETHGlobal の画面での操作（git の外）

### 4. ライブ審査中に `--live` は打たない（19:47）

> --live を審査中にやるか — 「やらない」

支払いは取り消せないので、審査中にお金を動かさない。実際に払った証拠は 09-05 の tx（Basescan）を見せる。

- 反映: 09-11 の `d2ad83d` が [`LIVE_JUDGING.md`](../LIVE_JUDGING.md) に記録し、1:30 の段を空撃ちと 09-05 の Basescan に書き換えた

### 5. 予測で話を進めず、まず実測とリサーチで確かめ、その結果に客観的な第三者のチェックを入れる（19:52）

- 反映（同日夜）: `86116eb` 第三者が LIVE_JUDGING.md を実物と 1 行ずつ照合して直した／`cdba5d8` PRIZES.md の応募経路と Bazantic 賞の要件を測り直した。どちらも本文に確かめたコマンドか実測値を持つ

## この日の設計と一次記録

- ETHGlobal の資格回答（09-10 18:46・公開スレッド）を一次記録にした: x402 Base subgraph を消費する SDK だけで資格がある。MCP 面は資格のためでなく差別化として残す（`096a029`）
- Bazantic の JWT について担当者から得た回答を一次記録にした（`d0346b3`・`d83e408`）
- 記録した数字は出どころと突き合わせる関門を `refresh-numbers` に置く。負のテストで赤を確かめてから戻す（`f076122`）
- SKILL.md の `pay_if_trusted` の手順は clean worktree で実測し、支払いには鍵と `viem/accounts` の両方が要ることを書く。決済経路・理由コード・依存は変えない（`3aeb407`）
- `/api/health` の劣化の経路を本番の記録から特定し、SDK が呼ぶ 2 経路（`/resources/{id}/decision`・`/payees/{addr}/score`）の判定には影響しないことを追跡した。コードは変えない（`e5e5914`）
