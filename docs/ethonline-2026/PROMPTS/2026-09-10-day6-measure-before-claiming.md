# 2026-09-10（Day 6）— 提出文の人称とヒューマナイザー・JWT と資格の一次回答・台本を実物で測り直す

ETHGlobal の規約は "all spec files, **prompts**, and planning artifacts" を求める。**その日の指示文を、その日のうちに、原文で置く。**

> **注記（2026-09-11 記載）**: このファイルは **09-11 に書いた**。当日に置く規則（[`README.md`](./README.md)）を破っている。
> §1 のオーナーの指示は、会話記録から機械抽出した verbatim（09-11 07:45 JST 抽出）。要約していない。画像つき・画像のみの行も数えている。
> §2 は各コミットの件名と本文から取った要約であり、AI→AI の指示の原文ではない。
> 対象コミット: `TZ=Asia/Tokyo git log origin/main --since='2026-09-10 00:00 +0900' --until='2026-09-11 00:00 +0900' --format='%h %s'`（15 件。§2 はこの出力の全ハッシュを 1 つずつ引く）。

---

## 1. オーナー（人間）からの指示 — 原文（JST 09-10）

（会話記録 `969328e1-9c9a-4ee7-850f-3d884fbf4ea7.jsonl` の `user` 行と `queue-operation` 行から 09-11 07:45 JST に機械抽出した verbatim。一字も変えていない。時刻は JST）

**06:41**

（画像 1 枚のみ・本文なし。画像は収載しない）

**07:03**

> ソロなので主語はIになるべきでは？

**07:04**

> AI的な記述ニュアンスが複数あるのでヒューマナイザーを通してください。

**07:08**

> 提出テキストにおけるヒューマナイザーの利用は必ずです。
> 公式文書の場合、カジュアルにする必要はありませんが、AIに書かせたと気づかれた場合一気に信頼を失います。
> ルール上は問題ありませんが、人間は常にそこを気にするし、テキストのAIっぽさの違和感はすぐに気づきます。

**07:26**

> check-in #2 もう出したよ

（画像 1 枚を添付。画像は収載しない）

**07:31**

> 絵文字は開発におけるフィーリングを問うものだよ。
> 信号の場合、注意が必要で困難にあたっていると人間は解釈します

**08:51**

> こちらにしてください

**18:48**

（画像 1 枚のみ・本文なし。画像は収載しない）

**18:50**

> 最終通し前にすべきことは？

**18:59**

> 鍵の取得方法

**19:47**

> --live を審査中にやるか — 「やらない」

**19:48**

> 招待メールってなに？

**19:52**

> ねえ、なぜあなたがいつもミスするのかをまだ全く理解していないように見える。
> 予測で話を進めるな。まず実測・リサーチで確認しろ。その結果に必ず客観的な第三者のチェックを入れろ。

## 2. AI が AI へ出した指示 — 要点（コミット順・JST）

### 2.1 未明 04:55 — `/api/health` の degraded の経路を確定（オーナー発言より前）
- `e5e5914` 02:59–04:00 JST の degraded 11 行はすべて `feedback_stats_unavailable(deadline:getLogsChunked)`（尾部走査の内側 2,500 ms 予算）。影響は agent-score 面に限られ、SDK が呼ぶ 2 経路（`/resources/{id}/decision`・`/payees/{addr}/score`）の判定は動かないと追跡。LIVE_JUDGING Q17 に言ってはいけない 3 点を記録。コード変更なし

### 2.2 朝 08:16–09:45 — Bazantic の JWT・英題の欠落が main を止めた件・数字の番人
- `d0346b3` Bazantic の Tom Hay の Discord 回答を記録（価格 0 の x402/MPP・Test Connection/Activate を 48 時間内に修正予定）。「JWT の迂回口は無い」の部分は同日 `d83e408` が撤回
- `1244663` `d0346b3` の日本語件名に英題が無く、root `npm test` と main の CI が赤になり全セッションの push が止まった。英題の欠落と索引の遅れを note に格下げ（`--strict` でだけ赤）。負のテストを追加
- `593c9e2` SUBMISSION_DRAFT §I の「2 回走らせるとブロックが進むのが見える」を削除（09-08 に同じブロックを 3 回実測済み）。受取人スコアを「安定」と書いた内部メモを「動く」へ
- `f076122` `recorded` の数字を出どころと突き合わせる `guard` を refresh-numbers に追加。sdk_mutations 44→45。負のテストで赤を確認してから戻した
- `d83e408` Tom Hay の JWT についての別々の 2 回答を分けて記録（我々の投稿への回答では JWT ヘッダは存在する）。`d0346b3` の「存在しない」を撤回し、SUBMISSION_DRAFT の開発者フィードバックも直した

§1 の 07:03–07:31 の指示（主語 I・ヒューマナイザー・絵文字）の対象は git の外の文面で、09-10 のコミット差分に `humaniz`・`ヒューマナイザ` は 0 件。

### 2.3 夜 19:27–20:57 — 最終通しの前に、書いてあることを実物で測り直す
- `3aeb407` SKILL.md の `pay_if_trusted` ブロック 11・12 は鍵だけでは動かず `viem/accounts` も要る（clean worktree で実測）。Build order に手順 5、`skill-live-check` が `module:` 前提を見る。文言のみで、決済経路・理由コード・依存は不変
- `2994716` 英語コミット索引の再生成
- `096a029` ETHGlobal の資格回答（09-10 18:46・公開スレッド）を PRIZES.md §1.1 に一次記録: x402 Base subgraph の消費だけで資格がある。MCP 面は資格のためでなく差別化として残す。LIVE_JUDGING に Q21。vet402.com セッションへ 10 コミットを申し送り
- `86116eb` 第三者が LIVE_JUDGING.md を実物と 1 行ずつ照合し、偽の記述 5 件・出典の無い記述 3 件を直した。停止時の画を `judge` で取り直し、変異数 45、**決勝の告知は Hacker Dashboard で招待メールは無い**（WINDOW_PLAN・SUBMISSION_DRAFT の 4 か所にも伝播）
- `78bf5e8` COMMITS_EN.md の再生成
- `5b128b5` `86116eb` の改名が「09-08 取得分」のファイル一覧と食い違っていたのを分けた
- `8d630d6` COMMITS_EN.md の再生成
- `cdba5d8` PRIZES.md が存在しないパスを Bazantic の応募経路に書き、賞ページ 8 要件のうち「結果の差を歩く動画」を落としていた。監視の見えない範囲・Arc の枠・57/56 ルートの違い・User-Agent 不要などを測り直して訂正
- `ecad214` COMMITS_EN.md の再生成

§1 との時間の並び: 19:48「招待メールってなに？」の後に、20:09 の `86116eb` が招待メールは無いと訂正した。19:52「予測で話を進めるな」の後の `86116eb`・`cdba5d8` は、本文に確かめたコマンドか実測値を持つ。

## 3. 人間の判断（この日）
- 提出テキストは一人称 I、ヒューマナイザーは必ず通す（07:03–07:08）。反映先は git の外の文面【未確認】
- check-in #2 を提出（07:26・ETHGlobal 側の操作で git の外）
- ライブ審査中に `--live` は打たない（19:47）。09-10 のコミットにこの判断を書いたものは無い（`LIVE_JUDGING.md` §1 #9 の行に「やらない」は 0 件・09-11 実測）
- 予測で進めず、実測・リサーチと第三者のチェックを必ず通す（19:52）
