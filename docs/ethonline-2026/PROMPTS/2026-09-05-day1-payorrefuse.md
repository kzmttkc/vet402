# 2026-09-05（Day 1）— payOrRefuse と I23

この日にオーナーが出した判断・指示と、それが入ったコミットの抜粋。会話の全文ではない（方針は [`README.md`](./README.md)）。時刻は JST。

## 判断と反映

### 1. 会期中は、会期スコープの実装をハッカソン戦略のセッションが持つ（08:31–08:33）

> ここからETH Tokyo完了まではハッカソンが優先になるので、目的を達成するために期間中にこちらで実装することは問題ありません。別セッションでの実装がハッカソン要件に悪影響を及ぼさないための措置です。

あわせて、vet402 に入れた実装や変更は、そのつどコア開発のセッションへ申し送る。

- 反映: `d2082cd` 申し送り台帳 [`docs/handoffs/CHANGELOG.md`](../../handoffs/CHANGELOG.md) を新設（以後、変更ごとに先頭へ 1 エントリ）

### 2. 監査の結果を分析し、現状と戦略ロードマップをコア開発へ渡す（08:14）

- 反映: `0ced8fe` 監査が効く 4 点と 30 日ロードマップの申し送り

### 3. 優先順に進め、人間の手番は手順つきで渡す（08:48・14:12）

> やるべきことは、優先順に進めて

オーナー自身がやる手番は、詳しい手順をつけて手番の一覧に入れる。

- 反映: 手番の一覧はこのリポの外。この日の実装は下の「設計」のとおり

## この日の設計（実装の発注に書いた要求）

- 先に red を書き、赤いことを確かめてから実装する。仕様はテストファイル `packages/sdk/test/pay-or-refuse.test.mjs`
- 拒否経路で signer に到達できないことを、呼び出し回数でなく Proxy のプロパティ参照 0 で示す。支払い処理は ALLOW ブランチ内の動的 import。fetch は引数で受け取る（テストが差し替える）
  - 反映: `d1f5378` payOrRefuse ／ `adfefeb` I23（カタログ外の売り手）を red で追加
- 買い手は facilitator を呼ばない。署名ヘッダを付けて売り手へ再送し、決済の結果は応答ヘッダ（`PAYMENT-RESPONSE` / `X-PAYMENT-RESPONSE`）から読む
  - 反映: `0832411`
- 「支払いは動的 import で、拒否経路では評価されない」を検算できるようにする。static import に書き換える変異で赤くなることを示してから戻す
  - 反映: `1930121`
- 決定の記録（F19/F20）は 1 本の追記専用 JSONL に入れ、行ごとの `source` で区別する。別ファイルに分けるとテストが何も証明しなくなるので、同じ store に混ぜて読み手が正しく分けることを要求する
  - 反映: `4078081`
- MCP の `pay_if_trusted` も判定が ALLOW のときだけ signer に到達する
  - 反映: `47937cb`
- The Graph の subgraph を第 2 の証拠源にする。Graph の鍵は呼び手が持つ
  - 反映: `51f7445`
- 呼び手は vet402 の ALLOW を要求しない設定を選べる。ただし BLOCK は外れない（WARN は意見、BLOCK は遮断）
  - 反映: `f6212eb`・`15e343f`
- A/B の成功条件を、走らせる前に事前登録する
  - 反映: `12af10c`（WINDOW_PLAN §16）

## この日に決めた会期の運用

- 長命ブランチ `ethonline-2026` を使わず、会期の境界はタグ `pre-ethonline-2026` 1 本にする
- 監査の是正のうち、署名文のドメイン束縛と settled の 2 層公開は会期中に入れる（`edd382a`・`ded69d6`）。署名鍵の隔離は会期後
- ETHGlobal 運営へ Continuity の追加開示を書面で送った（08:49・git の外）。数字は送る直前に実測した値を使った
- 規約が求める「どのファイルを AI が書いたか」に答える [`AI_USAGE.md`](../../../AI_USAGE.md) を新設（`7c19142`）
