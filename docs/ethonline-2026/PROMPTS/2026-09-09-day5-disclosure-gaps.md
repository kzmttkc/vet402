# 2026-09-09（Day 5）— 開示の規則との突き合わせ・動画 v4

この日にオーナーが出した判断・指示と、それが入ったコミットの抜粋。会話の全文ではない（方針は [`README.md`](./README.md)）。時刻は JST。

## 判断と反映

### 1. 各プロジェクト・トラックの要件に合っているかを精査し、受賞確率を上げる新提案を出す。第三者視点のダブルチェックを入れる（06:13）

- 反映: 主催者の開示規則（`ethglobal.com/rules`）と提出物を別の監査役に突き合わせさせ、3 件をそろえた——`52fe043` 会期開始を 00:00 UTC と書いた 2 つの記録に、正しい開始時刻（16:00 UTC）の注記を添える（本文は消さない）／`8589d12` 09-08・09-09 の指示記録をこのディレクトリに置く／GitHub Release `pre-ethonline-2026` の本文を README と同じ記述にそろえる（git の外・タグは動かさない）

### 2. 新提案をすべて実装し、動画は冒頭で「続きを見たい」と思わせる構成に組み直す（07:29）

> 動画は審査に大きく響きます。一日に数十以上の動画を見なければならない審査員にとって退屈な作業で、その中で目を引くにはショート動画のバズリロジックと同じで冒頭で「続きを見たい！」と思わせる必要があります。単なる事実の羅列ではなく、しっかりと起承転結をいれた構成に組み直してください。

録音は S1 だけでなく全シーンを取り直すので、原稿をもう一度点検して最適化する。実装は慎重に、常にダブルチェックを入れる。

- 反映: オーナーが S1〜S10 を録り直し（11:53–13:34）、提出動画 v4 に差し替えた（git の外）。v4 の実測タイムラインは `243da16`

### 3. Bazantic の JWT はオーナーが渡したものを使う（08:28）

- 反映: `7b5c58d` Bazantic のゲートウェイを 09-09 に測り直した記録（無料ツールは未払いで応答する・JWT の件への回答）

### 4. 提出前の点検: コード・秘密情報・全機能の可用性を、客観的かつ敵対的にダブルチェックする。審査で穴を突かれず、品質を評価されるように（13:52）

- 反映: `a783dab` A/B の秘密フィルタを広げ、本番 DB のホスト名を公開文書に書けない関門を置く／`690b4fc` 第三者の私信とローカルパスを公開文書から外し、デモの依存版を固定／`4b281ab` 境界表に amount の文字列 `"0"` を足し、変異 M45 で赤くなることを確認

## この日の設計（発注に書いた要求）

- 実装役への発注の冒頭に 2 行を必ず置く（`219ba1e` の型）: 「この発注の前提のうち最も怪しいものを 1 つ挙げ、実装前に確かめよ。違うなら実装せずに報告せよ」「決済経路を変える必要が出たら、変えずに止まって報告せよ」
- 指示記録を書くときは `git log --since=2026-09-08` と突き合わせ、コミットに無い作業を書かない・在る作業を落とさない。数字を手で書くならコマンドを横に置く
- `/api/health` の劣化行には、どの経路が投げたかを `detail` に載せる。決済経路には触らない（`64a6eb2`）
- `/status` には測ったことだけを書く（`4c99fb2`）
