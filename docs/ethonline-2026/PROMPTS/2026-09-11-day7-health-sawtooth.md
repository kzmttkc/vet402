# 2026-09-11（Day 7）— A/B v2 の実行・主語の規則・審査日に正しい文

この日にオーナーが出した判断・指示と、それが入ったコミットの抜粋。会話の全文ではない（方針は [`README.md`](./README.md)）。時刻は JST。

## 判断と反映

### 1. `/api/health` の散発的な劣化は、審査員が叩いた瞬間に当たりうる。0 にできないか（06:20）

> 審査員が叩いた瞬間に当たる確率は、この区間で言えば 2/12 ≒ 17%
>
> これは0にはならないの？

- 反映: 本番の記録を読み取りだけで引いて原因を追った（`d60d73c`・`2d5f78e`）。原因は確定しなかったので、本番の cron・deadline・コードは変えていない（本番は 2026-09-15 まで凍結）。ライブ審査では health の detail を読み上げず、時間帯ごとの劣化率の実測だけを言う（`0c8b01c`）

### 2. A/B v2 の 1 回実行を承認。鍵はクリップボードで渡す（08:03）

承認の対象: Anthropic API の費用 $10〜45（見込み）で A/B v2 を 1 回だけ走らせること。採点表は変えず、実行前にその理由と予測をコミットしてから走らせる。

- 反映: `91898a2` 実行前のメモ（F4 の正解を固定・payer 鍵を渡さないので署名 0・tx 0）／`79488a1` 生ログ: A 7/10・B 5/10、102 tool calls・0 tx。事前の予測「F4 は B で直り A では直らない」は外れ、そのまま記録した

### 3. 固有名の誤記は「原文のまま」にこだわらず、指摘したうえで直す（08:36）

> この場合修正はすべきなので、「原文をそのまま載せる」にこだわるべきではなくて指摘したうえで直すが正しい

- 反映: `c39f4a3` 誤記を〔原文: …〕つきで直す規則を README に書いた（この規則は現在の [`README.md`](./README.md) にも残している）

### 4. 提出物の主語: 人が決めたことは I、システムがすることは vet402 / it。AI エージェントを we に含めない（13:35–13:47）

> ハッカソンでweと書いた場合、読み手は「他のチームメンバー」と読み解くでしょう。

> AIを使いましたかと聞かれればハイ使いました。となるし、その時でもI & AI Agentというイメージです。
> とにかくこの場面では主体は変更可能な部分はIに統一してください。

- 反映: `fa10182` 審査員向けの英文を一人称単数に／`04a186b` ライブ審査の台本も I に／`bbf06d7` デモの画面の文字列と MCP ツールの説明から we / our を外す（判定・理由コード・数字は不変）／`b109531` README の Continuity 節の見出し

### 5. デモ動画は v4 で確定し、これ以上撮り直さない。ショーケース画像 6 枚とカバーの差し替えを承認（14:32–14:34）

差し替えの方針は、会期前からある画面ではなく、会期中に作ったものを写すこと。

- 反映: ETHGlobal のショーケース（git の外）

### 6. ETHGlobal へ、Final checklist の文言と Continuity での提出、ETHGlobal Tokyo への出場を確認する（14:32–14:39）

オーナーが Discord で運営に確認した。

- 反映: `f0e1c20` 運営の回答を PRIZES.md に一次記録（Continuity での提出は問題なく、vet402 の新しい作業で Tokyo の Continuity トラックに出ることも問題ない）

### 7. 対外の文は、読まれる日（審査日）に正しいように書く（14:37・15:00）

原稿は提出日と審査日から逆算して作る。画像に写った相対時刻「3 days ago」のように、読まれる日に古くなる表記を残さない。動く数値や内容は、その時点の値に日付を付けて固定するか、提出の直前に変数へ入れるかの二択。

- 反映: `c057f79` Bazantic の $0 ルートの発見に日付を付け、確かめた範囲（57 ツール中 12 本）だけを書く／`c864e86` ライブ審査の台本の動く行すべてに日付を付ける

### 8. ライブ審査の準備は、話す人の負担（画面で何を見せるか・打つ操作・文字の大きさ）まで数えて揃える。翌日に練習するので台本・手順・材料をすべて用意する（14:42–14:52）

- 反映: 練習用の台本と手順はリポの外

## この日の設計（発注に書いた要求）

- 審査員として clean clone を歩き、SKILL.md の `pay_if_trusted` ブロックを貼ってそのまま動くようにする。鍵は未入金のものをシェルで作り、`signTypedData` 0 回を実測する（`276cdae`）
- LIVE_JUDGING.md の画面のコマンドを、本番に対して空撃ち（`--live` なし）で打ち直し、画面の期待値をその日の出力に合わせる。構成・順番・時間は変えない（`780be69`）
