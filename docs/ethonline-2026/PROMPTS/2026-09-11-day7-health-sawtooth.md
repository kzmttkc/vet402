# 2026-09-11（Day 7）— `/api/health` の degraded は日次 cron の鋸歯

ETHGlobal の規約は "all spec files, **prompts**, and planning artifacts" を求める。**その日の指示文を、その日のうちに、原文で置く。**

> **抽出時点 09-11 07:45 JST。** この時点までの分だけを置いている。**締切前の最終通しで同じ抽出を再実行し、この日の残りを追記する。**
> 対象コミット: `TZ=Asia/Tokyo git log origin/main --since='2026-09-11 00:00 +0900' --format='%h %s'`。抽出時点で 1 件（06:44）。同日にこのファイル自身を足すコミットが続く。

---

## 1. オーナー（人間）からの指示 — 原文（JST 09-11）

（会話記録 `969328e1-9c9a-4ee7-850f-3d884fbf4ea7.jsonl` の `user` 行と `queue-operation` 行から 09-11 07:45 JST 時点で機械抽出した verbatim。一字も変えていない。時刻は JST）

**06:20**

> 常時劣化しているわけではない。散発で、1〜2件が数分続いて戻る
> 審査員が叩いた瞬間に当たる確率は、この区間で言えば 2/12 ≒ 17%
>
> これは0にはならないの？

**07:28**

> 現在進めるべきことは？

## 2. AI が AI へ出した指示 — 要点（コミット順・JST）

（コミットの件名と本文から取った要約であり、原文ではない）

- `d60d73c` 06:44 — 06:20 の問い「これは0にはならないの？」の後のコミット。本番 `health_snapshots` を読み取りだけで引き、`deadline:getLogsChunked` の degraded 85 行のうち 81 行が 18:00–01:59 UTC にあり、02:00–09:59 UTC は 2 日とも空と記録。日次 cron `0 2 * * *` で checkpoint が 1 日 1 回しか進まず、要求経路の尾部走査が次の実行まで広がり続けて 2,500 ms を超える形で、上流 RPC の遅延ではない。`detail` 列は 09-08 まで NULL で、09-09 の急増は名前が付いた日であって始まった日ではない。本番の env・cron・コード・DB は不変（2026-09-15 まで凍結）

## 3. 人間の判断（この日）
抽出時点では無し【未確認】。
