# 2026-09-04（Day 0）— 失敗テストを red で置く

この日にオーナーが出した判断・指示と、それが入ったコミットの抜粋。会話の全文ではない（方針は [`README.md`](./README.md)）。時刻は JST。

## 判断と反映

### 1. 受賞確率を上げるために、会期の計画を複数の目で確かめる（09-03 21:03・会期前夜）

> 戦略、手順に漏れや抜け、がないか、要件に沿っているか、受賞確率を一％〔原文: 位置％〕でも上げて最大化するには何が必要か、複数のエージェントで綿密確認してください。

- 反映: `1bc9129` 会期の正典を [`WINDOW_PLAN.md`](../WINDOW_PLAN.md) 1 枚に確定し、失敗テストを 22 本にした

### 2. World AgentKit 枠を取り下げる（09-03 22:27）

必要な World ID の登録を示す証拠を、会期中に手元へ揃えられない。

- 反映: `1bc9129`（World の枠を切り、支払い先を The Graph 本体にした）

### 3. The Graph との過去の関係に便宜を求めない（06:13）

> 便宜を使うつもりはない。元コミュニティマネージャーがハッカソンに参加した。それだけ。他と同じに扱う。

- 反映: `4437da3` WINDOW_PLAN §1.5 に行動の縛りとして明記（便宜を求めない・パートナー窓口では誰でも聞ける要件確認だけ・提出物で関係を売り文句にしない）

### 4. 会期前の作業の開示は、公開チャンネルでなく運営の担当者へ書面で出す（06:38–06:51）

公開の Discord で自己開示はしない。まず担当者に提出の仕方を聞く。

- 反映: 開示は 09-05 に運営へ書面で送った（git の外）。送った本文の写しは [`DISCLOSURE_2026-09-05.md`](../DISCLOSURE_2026-09-05.md)

## この日の設計（テストで先に固定したこと）

WINDOW_PLAN §4 の 22 項目をテスト名にし、実装はせず、全部落ちている状態で 1 コミットにする。

- 拒否のとき signer に「到達できない」ことを 4 層で示す: ① account を Proxy で包み `sign*` へのプロパティ参照 0 ② fetch を許可リストにし facilitator / RPC / Graph へ出たら throw ③ 支払い実装は ALLOW ブランチ内の動的 import ④ ネガティブコントロールとして ALLOW で `signTypedData` がちょうど 1 回出ること
- 判定源は `/decision?role=payer`。既定 policy は `allow_only`
- テストは「落ちたこと」だけでなく理由コードの中身と判定を引いた回数まで検査し、未実装のスタブでは通らない形にする
- 反映: `e68cc57`（SDK 21 本・MCP 3 本、すべて red）
