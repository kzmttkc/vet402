# 2026-09-04 · Day 0 — 失敗テストを red で置く

> 規約 "include all spec files, **prompts**, and planning artifacts". これはその日の指示文の**原文**。要約も編集もしない。

## オーナー（Takeshi）からの指示（原文・時系列）

2026-09-11 に会話記録から機械抽出で全件に差し替えた（当初は当日に選んだ抜粋だった）。

（会話記録 `969328e1-9c9a-4ee7-850f-3d884fbf4ea7.jsonl` の `user` 行と `queue-operation` 行から 2026-09-11 に機械抽出した。時刻は JST。固有名・数字の明らかな誤記は直し、〔原文: …〕で元の表記を示した（[`README.md`](./README.md)）。それ以外は一字も変えていない）

**06:13**

> 便宜を使うつもりはない。元コミュニティマネージャーがハッカソンに参加した。それだけ。他と同じに扱う。

**06:38**

> ① 運営への追加の書面開示 -どのDiscordのどこに貼るの？提出場所は本当にDiscordで合ってる？

**06:40**

（画像 1 枚のみ・本文なし。画像は収載しない）

**06:42**

> Discordで担当者に提出の仕方を聞く内容で文面を作成して

**06:51**

> そもそもそんなことをDiscordで公に開示しているやつはいない。

**11:44**

> どこまですすんだ？

（画像 1 枚を添付。画像は収載しない）

**16:36**

> ここまで完璧ですか？

**16:41**

> 忘れるという無様を二度と繰り返さないように構造化して先に進んでください。
>
> 1,2,3,4を完璧に潰して

**16:53**

> 送った

**前日 09-03 の発言（2 件）** — 09-03 は会期前で、このディレクトリに日付ファイルが無い。当初版がこの 2 件を載せていたので、同じ会話記録の原文で残す（09-03 の全件ではない）。

**09-03 21:03**

> 明日 09:00 の解禁通知から勝負です。
> しっかり戦略、手順に漏れや抜け、がないか、要件に沿っているか、受賞確率を一％〔原文: 位置％〕でも上げて最大化するには何が必要か、複数のエージェントで綿密確認してください。

**09-03 22:27**

> ・オーブに行って虹彩登録をしたことがあるがその証拠が手元にはない。他人に預けたデバイス内にある。会期中は間に合わない。

**注記（当初版の地の文。オーナーの発言ではない。「AI…実装指示」「結果」に同じ内容が無いので残す）**

- 09-03 22:27 の発言 → World AgentKit 枠を取り下げ
- 09-04 06:13 の発言 → WINDOW_PLAN §1.5
- 09-04 06:51 の発言 → 公開チャンネルでの自己開示を取り下げ

## AI（ハッカソン戦略セッション）が自分に出した実装指示（Day 0）

WINDOW_PLAN.md §4 の22項目をテスト名にする。**実装はしない。全部落ちている状態で1コミット。**

拒否のとき signer は「呼ばれなかった」のではなく到達できない、を4層で証明する:
1. account を Proxy で包み、`sign*` へのプロパティ参照が0（回数では足りない）
2. fetch を許可リスト方式にし、facilitator / RPC / Graph へ出たら throw
3. 支払い実装は ALLOW ブランチ内の動的 import
4. ネガティブコントロール——同じハーネスで ALLOW を通し `signTypedData` が「ちょうど1回」出ることを示す

判定源は `/decision?role=payer`。旧 `/payees/{addr}/score` の signals は読まない。既定 policy は `allow_only`。

## 結果

- コミット `b366921`（`ethonline: test(sdk,mcp): …`）SDK 21本・MCP 3本、24本すべて赤。
  `b366921` は廃止ブランチ `ethonline-2026` 上のハッシュで、**main では `e68cc57`**（`ethonline: test(sdk,mcp): payOrRefuse の fail-closed 契約を red で置く（Day 0）`、2026-09-05 08:47 JST）。ブランチを消しても main で引ける
- 書いた直後に2本が実装なしで緑になった（`assert.rejects` がスタブの throw で通る／書き込み検査が無動作でも通る）。
  理由の中身と「判定を1回引いたこと」を要求する形に直して 21/21 赤へ戻した
