# 2026-09-07（Day 3）— 承認・A/B 実走・新提案の採用

この日にオーナーが出した判断・指示と、それが入ったコミットの抜粋。会話の全文ではない（方針は [`README.md`](./README.md)）。時刻は JST。

## 判断と反映

### 1. 追加実装・外部送信・Recipe 公開を承認し、A/B 再実行の鍵を渡す（06:13）

> ・日々統計が進むので数字の誤差は気にしなくていいし、必要なら提出の直前に数字だけ拾って変えれば良い。
> ・追加実装承認
> ・外部送信OK
> ・Recipe公開承認

承認の対象は 3 件: Bazantic の Recipe 公開（AQ-052）、判定 API の鍵なし読み取り枠（AQ-053）、The Graph への公開の質問（AQ-054）。A/B 再実行に使う API 鍵はオーナーが渡し、子プロセスの環境変数にだけ置いた（ディスクにも記録にも残さない）。

- 反映: `6a0868e` Recipe を公開版へ／`3738890` `/decision` の鍵なし枠（IP ごと 10 回/分・本文は鍵ありと同一）／`b10f19b` A/B 本走の生ログ（`claude-opus-5`・事前登録どおり 1 回）／`3d622f6` 動く数字を印で機械可読にし、提出直前に `npm run refresh-numbers` 1 本で拾い直す
- 外部への質問の送信は git の外

### 2. 実装から見えた気づきと新提案を出させ、全部採用する（06:42–12:18・4 回）

> 新提案すべて採用。現在の実装ルールを踏まえて一つ一つ確実に進めてください。

採用して入ったもの:

- CI の関門: SDK の変異テストと clean checkout の `judge-check` を main の関門にする（`ea313f6`）
- 判定 API が呼び手の policy を受け取り、SDK と同じ語で `caller_policy` を返す（`9035a3b`・`08c6d0f`・`1c15d6b`・`b40e3cd`）。既定値・順序・語彙は両方の実装を実際に呼んで突き合わせる（`e630f85`）
- 提出物の数字は `npm run metrics` 1 本で `trials.jsonl` から再採点して印字する（`f6c3248`）
- 動画の台本を前倒しで用意する（`3ea2c99`）
- main へ入れる手順を `scripts/push-main.sh` 1 本にする（`8ea9fe9`）
- `refuse()` の理由語を型で縛り、型に無い語はコンパイルで止める（`4539e51`・`bf1702d`）
- 4 回目（12:18）: README 最上部に審査員が 60 秒で動かせる 3 コマンド（`f088918`）、git で出る数字はその場で導出して比べる（`f31bc07`）、ライブ審査の台本と想定問答（`31f8cbd`）、提出フォームの文章を翌日読めるように下書き（`f9a4fae`）
- A/B v2 は実データを見る前に事前登録する（`138e149`・WINDOW_PLAN §16.5）

### 3. Project check-in #1 と Discord の Project Check-ins への投稿は、送る前にオーナーが確認して承認する（19:04–19:26）

> 貼ってよい。

投稿文は humanizer を通し、自然な文にしてから出す。

- 反映: `a1915b2` check-in #1 の開放を記録。送信は ETHGlobal の画面で、git の外

### 4. 動画のナレーションはオーナー本人の声で、09-08 から録音する（20:23–20:43）

- 反映: `3ea2c99` 台本と撮影表

### 5. 提出前の点検: コード・秘密情報・全タブの可用性・文章の質を、客観的かつ敵対的にダブルチェックする。対象はサイト・提出物・GitHub のすべて（20:48–20:49）

> ・テキストのクオリティチェック（無駄をなくしつつ、最大限に理解できる最適解に）
>
> 客観性、敵対性など非主観で。

- 反映: `3648d3c` 審査員向け文書の修正／`24f9197`・`8f554ee`・`1dece66` 個人の連絡先や認証情報を出さない・鍵ファイルを追跡しない／`2440de5`・`f5bbec9` 本番依存の high を 0 に／`abebeed` サイトの表記の整合／`8054047`・`a975d77`・`cbb9169`・`54b07a0` 署名の前に 402 の額・policy の値・再送先を検査する

## この日の設計（発注に書いた要求）

- 鍵なし判定枠: census/summary と同じ部品を使い、二重実装しない。本文は鍵ありと同一。env フラグで即時に戻せる。変異 3 本（IP を無視／11 回目も 200／鍵ありにも枠がかかる）で赤くなることを示す
- SDK は鍵が無くても先回りして throw しない。サーバの 401 をそのまま返す（`c796ebd`・`7c7ed8f`）
- DX: まず審査員として clean clone を歩き、詰まった箇所を全部挙げてから直す。数字は実走で取る（`a12fa9f`・`6583102`・`6daa707`）
- CI の関門は、赤くなることの証明（検査を 1 つ外して exit 1）を添えて入れる（`ea313f6`）
- 判定 API の policy: 語は SDK と 1 字も違えない。`recommendation` は変えない。クエリ無しの応答は従来と完全一致をテストで固定する。subgraph の床はサーバでは扱わない（呼び手の鍵を持たないため）
