# 2026-09-07（Day 3）— 承認4件・A/B 実走・「新提案すべて採用」

ETHGlobal の規約は "all spec files, **prompts**, and planning artifacts" を求める。**その日の指示文を、その日のうちに、原文で置く。**

---

## 1. オーナー（人間）からの指示 — 原文（JST 09-07）

> ・日々統計が進むので数字の誤差は気にしなくていいし、必要なら提出の直前に数字だけ拾って変えれば良い。
> ・追加実装承認
> ・外部送信OK
> ・Recipe公開承認
> ・Anthropicの鍵については今コピーから拾って

> 今回の実装から見えた気づきや反省、そこから生まれる新提案はありますか？

> 新提案すべて採用。現在の実装ルールを踏まえて一つ一つ確実に進めてください。

**1つ目で承認3件（AQ-052 Recipe 公開・AQ-053 判定 API の鍵なし枠・AQ-054 Graph への公開質問）と A/B 再実行の鍵**。
鍵はクリップボードから子プロセスの環境変数へだけ渡した（ディスクにも AI の文脈にも残さない。前日の Graph 鍵漏洩と同型の経路を避ける）。
**3つ目で採用された新提案4件**: CI 関門（変異＋clean checkout）／判定 API に呼び手 policy／手番の突合ゲート／会期後の公開ベンチ。

## 2. AI が AI へ出した指示 — 要点

- **A/B 実走**（人間の鍵・1回だけ・事前登録どおり回し直さない）: `claude-opus-5`・effort high・橋あり。結果 A 5/10・B 5/10。生ログを `docs/ethonline-2026/ab/2026-09-06T213134Z/` へ。
- **鍵なし判定枠**（sub-agent・`3738890`）: 「census/summary と同じ部品を使う。二重実装しない。本文は鍵ありと同一。env フラグで即時に戻せる。4面パリティ。変異3本（IP 無視／11回目も 200／鍵ありにも枠）」。本番で 200 → 10 回目から 429 を実測。
- **鍵1本化**（sub-agent・`7c7ed8f`）: 「SDK は先回りして throw しない。サーバの 401 をそのまま。demo の env 行に `keyless` と出す。変異: `Bearer undefined` を付ける／demo が鍵なしで止まる」。
- **DX**（sub-agent・`6daa707`）: 「まず審査員として clean clone を歩き、詰まった箇所を全部列挙してから直す。数字は実走で」→ clean clone では ab が viem 不在で赤 2 と判明 → 直接依存へ。
- **CI 関門**（sub-agent・`ea313f6`）: 「赤くなることの証明を貼る（変異1つの検査を外して exit 1・ab のテストを1本壊して exit 1）」。
- **手番の突合**（sub-agent・管理リポ `f98e8bb`）: 「正典（JSON）と TODO の見出しを突合し、欠けたら叫ぶ。`--check` で exit 1。規律でなく計器」。
- **判定 API の policy**（sub-agent・実装中）: 「語は SDK と1字違わず。`recommendation` は不変。クエリ無しは従来応答と完全一致をテストで固定。subgraph の床はサーバでは扱わない（呼び手の鍵を持たない・§1.5）」。
- **Recipe 公開の反映＋フィードバック doc**（sub-agent・`6a0868e`・`1a3913a`）: 「`state === "draft"` 固定のテストを両方向の規則へ。数字は trials.jsonl から再計算し、コマンドを末尾に」→ 担当が私の語彙指標の過小評価（6%/31%）を見つけ、59%/91% に訂正。

## 3. 人間の判断（この日）
承認3件・鍵の手渡し・数字の扱いの方針（提出直前に一括で拾う）・新提案4件の採用。

---

## 4. 2回目の「新提案すべて採用」（同日 07:53）— 原文

> 今回の実装から見えた気づきや反省、そこから生まれる新提案はありますか？

> 新提案すべて採用。現在の実装ルールを踏まえて一つ一つ確実に進めてください。

採用4件と AI→AI 指示の要点:
- **ゲートウェイ差分カナリア**（管理リポ `scripts/bazantic_gateway_drift.py`）: 「openapi の `parameters[].name` と MCP `tools/list` の `inputSchema.properties` を突合。Bazantic が足す共通キーは除外リストとして state に持つ。差 0 なら黙る。ネットワーク失敗は『測れなかった』と別語で記帳し 0 件と混同しない」。実装中に除外キー `Idempotency-Key` が偽陽性と判明し除外から外した
- **既定値のパリティ検査**（`tests/caller-policy-sdk-parity.test.ts`）: 「両方の実装から値を読む。片方をハードコードしない。順序は同じ入力で同じ最初の理由コードが出ることを両方を実際に呼んで比べる」→ SDK の型 `PayRefuseReason` に `payee_recommendation_block` が無い抜けを発見
- **数字は1本のスクリプトから**（`npm run metrics`）: 「summary.json を信じず trials.jsonl から再採点。語彙集合はリポのファイルから読む。文書の数字と1つずつ突合し、不一致は直さず報告」→ A の語彙率 19/32 → 20/32 の不一致を発見（大小の正規化）。文書をスクリプトの値に統一
- **動画台本の前倒し**（`VIDEO_SCRIPT.md`）: 「数字はプレースホルダにして撮影日に埋めるコマンドを添える。口で言う数字は2つに絞り下限で読む。The Graph のウォレットが WARN と出る絵は、先に『我々の欠損であって売り手の落ち度ではない』と言う」

