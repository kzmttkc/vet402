> **STALE — do not copy these numbers.** Hand-written on 2026-08-25 and never updated.
> The L1 figures below (1,512 attempts / 601 settled) were superseded within days.
> As of 2026-09-05 09:27 (`GET /api/v1/observatory/state`): **3,241 attempts / 1,629 settled**
> (**71 nonce-bound / 1,558 amount+payee only**) **/ 1,444 delivered**. Never quote `settled` without that split.
> Two further cautions before reusing anything here, both measured 2026-09-05:
> - `settled` is **not** one evidence tier. 1,558 of 1,629 (95.6%) are the older, non-nonce-bound rule.
>   `/observatory/state` now publishes both counts (`l1.settledNonceBound` / `l1.settledAmountPayeeOnly`,
>   shipped 2026-09-05). Publish both, never the total alone.
> - `/api/v1/observatory/history` was re-rolled on 2026-09-05 and now sums to exactly 3,241 / 1,629,
>   with `coverageFrom` / `rolledUpThrough` / `semantics` in the response. Both numbers may now appear
>   in one document; cite the retrieval date and the coverage window.
>
> Kept only as the record of what was said on 2026-08-25. For a current post, use the generator
> (`scripts/vet402_weekly_facts.py` in the ops repo) **after** its denominator fix lands.

# Weekly facts draft — 2026-08-25

> Human-approve. Do not auto-post from this repo.
> Retrieved `2026-08-25T00:48:55.088Z` from `GET https://vet402.com/api/v1/observatory/state`.
> Re-curl before posting. Fail closed: if a key is missing, drop the line.

```
vet402 observatory 2026-08-25

catalog active 15275 / tracked 18832  (incl. testnets)
L0 published pass 1127 · unverified 17705 (not dead)
L0 coverage 7d 18.1% of active
L1 1512 attempts · 601 settled on-chain · 911 not settled
chain (mainnet only): Base 1073/14890 · Solana 41/217

source: https://vet402.com/api/v1/observatory/state
```

Notes for the owner:

- `publishedFail` is L0 (`0`). Do not call that “failures.” L1 not-settled is `1512 − 601 = 911`.
- `byChain` is a **list** (`{chain, activeEndpoints, publishedPass, …}`), mainnet only. Top-level totals include testnets.
- Magicians #380 (same day) used older L1 (1233 / 531). If you post this, the L1 counts moved. That is allowed; do not silently keep the old ones.
- Magicians went out today. Skip X if you want one public sentence per day.
